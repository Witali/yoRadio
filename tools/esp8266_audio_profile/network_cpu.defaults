# Append after the canonical native sdkconfig.defaults in a fresh build.
# Do not use CPU_CLK: its 32-bit runtime counter wraps during a 20 s test.
CONFIG_FREERTOS_USE_TRACE_FACILITY=y
CONFIG_FREERTOS_GENERATE_RUN_TIME_STATS=y
CONFIG_FREERTOS_RUN_TIME_STATS_USING_ESP_TIMER=y
