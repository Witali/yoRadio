interface:
  display_name: "Flash YoRadio ESP32 board"
  short_description: "Build, flash, and reset the CYD2USB board"
  default_prompt: "Use $flash-reset-esp32 to build and flash YoRadio with DAC or PDM audio output."

policy:
  allow_implicit_invocation: true
