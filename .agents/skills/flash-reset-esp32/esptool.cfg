[esptool]
# Verified CH340C profile copied from the HLV Codec board workflow.
# Hold reset for 200 ms, switch DTR first and RTS immediately after it, then
# keep GPIO0 low for 100 ms before returning both control lines to idle.
custom_reset_sequence = D0|R1|W0.200|D1|R0|W0.100|D0|R0
