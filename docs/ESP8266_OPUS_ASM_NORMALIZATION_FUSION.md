# Opus ASM: прототип объединения normalization и collapse mask

2026-09-13. Пункт 4 основного ASM-плана. Добавлен экспериментальный
`bands-fused-asm`; физический A/B завершён, кандидат отклонён. Исходный C fallback и GCC ASM
snapshot не изменены. Обычный профиль не переключён на эксперимент.

## Установлено по коду

alg_unquant сначала получает весь iy и Ryy из decode_pulses, нормализует
N значений в X, выполняет exp_rotation над X и отдельно читает iy для маски.
Последний проход не нужен при B<=1: исходник сразу возвращает маску 1.
При B>1 и делимости N на B он повторно читает N значений iy.

Маску можно вычислять по блокам во время первого чтения iy. Коэффициенты
нормализации рассчитываются один раз, после получения полного Ryy.
Изменять порядок entropy decoding или выдавать частичный результат до Ryy нельзя.

Для ASM важно сохранять RAW 32-битный iy для OR перед MUL16 и округлением.
Маску нельзя вычислять из X: при gain=0 или малом gain X может быть нулевым,
хотя соответствующая полоса содержит ненулевые импульсы. Доступ к iy в IRAM
остаётся выровненным 32-битным, запись X — исходной 16-битной.

## Проверенный локальный прототип

Тест включает настоящий pinned vq.c и использует его normalise_residual и
extract_collapse_mask как эталон. Отдельная fused-функция сохраняет fixed-point
вычисления g/k и объединяет циклы для B>1, N%B==0. Остальные формы используют
исходные функции: не допускается потеря остатка N%B.

- 8000 сочетаний N/B/gain/векторов, включая редкие одиночные импульсы,
  отрицательные значения, амплитуду 32767 и детерминированные смешанные данные.
- 2880 случаев исходного пути, 1596 случаев нулевого PCM с ненулевой маской.
- Нормализованные значения и маски совпадают точно; iy не изменяется,
  защитные области выхода сохранены; ASAN/UBSAN пройдены.
- Это unit-проверка арифметики, **не полное декодирование корпуса и не
  подтверждение скорости или размещения регистров будущего ASM**.

Запуск: node --test tests/esp8266-opus-fused-normalize-prototype.test.js.
Результат с хешами: firmware/development/esp8266-opus-fused-normalize-prototype/result.json.

## Следующие шаги

- [x] Реализовать отдельный ASM overlay alg_unquant; оставить остальные функции
      и C fallback неизменными.
- [x] Выделить путь B<=1 без лишнего OR на каждый элемент; неизвестные формы
      сохранить на исходном пути.
- [x] Сохранить маску через exp_rotation без роста стека. В текущем ASM
      frame=112 B; используется ранее незанятый slot sp+72, не a13: в части
      rotation a13 временно хранит X и сохраняется в sp+40. Проверка генератора
      запрещает применение к snapshot, где sp+72 уже используется.
- [x] Не добавлять деление или проверку B на каждый sample. Оценить затраты
      внешнего цикла/счётчиков против удаляемых чтений и переходов.
- [x] Проверить регистры, память, весь host PCM-корпус/PLC/reset/OOM, размеры
      object-секций. Полный linked ELF и реальные показатели проверяются на плате.
- [x] Выполнить минимум 10 физических A/B каждого варианта без profiler.
      Только измерения решат, выгодно ли слияние в целом.

## ASM-кандидат

Рецепт `tools/esp8266_opus_asm/fused.cjs`, инструкции с описанием ABI —
`fused_normalize.inc.s`. Прямой путь для B=2/4/8 при N>=B и N%B==0.
Коэффициенты g/k/bias вычисляет исходный GCC-код один раз; fused loop читает
каждый iy через L32I, вычисляет OR и исходные MUL16S/round/shift/store.
Вызовов, выключения прерываний, новых таблиц и heap-буферов нет. SAR setup
внутри sample loop оставлен как у контроля (его отдельный hoist уже проигрывал).

Для B<=1 и custom shapes сохранён исходный loop и поздний проход. Маска -1
в sp+72 обозначает fallback; готовая маска 0..255 переживает все rotation calls.
Размер object-функции 806 -> 916 байт; data/bss и остальные функции точны.
Проверены 10000 shape trials инструкции за инструкцией (недопустимые B>N
не запускаются), границы выхода/IRAM word reads/live registers и raw iy
с ненулевыми старшими битами при нулевом PCM. Полный host corpus 12/24/64/128/192,
PLC/reset/OOM и guards точен с ASAN/UBSAN. Это не заменяет physical PCM/CPU.

## Почему область применения оказалась небольшой

Добавлен воспроизводимый host-only `tools/esp8266_opus_asm/profile_shapes.cjs`.
Он заменяет только private host object vq, не меняет исходники/прошивку и
считает один обычный decode каждого fixture (без self-test/reset-повторов).
Все 5 PCM совпадают с непротрассированным эталоном. Полнота распределения
проверяется отдельным тестом. Это распределение C-путей, не время LX106.

| Fixture | alg_unquant calls | B=1 calls | Fused eligible calls | Eligible coefficients / all | Rotation calls |
|---|---:|---:|---:|---:|---:|
| Mono 12 | 0 | 0 | 0 | 0 / 0 | 0 |
| Mono 24 | 58 | 54 | 4 | 112 / 1212 | 42 |
| Stereo 64 | 512 | 416 | 82 | 1024 / 8044 | 225 |
| Stereo 128 | 1074 | 837 | 231 | 2210 / 10272 | 127 |
| Stereo 192 | 1648 | 1360 | 288 | 2324 / 12752 | 69 |

На 192 кбит/с B=1 занимает 82,52% вызовов; дополнительного прохода маски
там изначально нет. Экономия повторного чтения относится только к 18,22%
коэффициентов. iy лежит в word-scratch/IRAM: это не уменьшение чтения
flash-констант. Новые guards/код добавлены при относительно коротких векторах.
Этого достаточно понизить приоритет идеи, но не доказать источник всего
замедления: вклад компоновки/flash cache отдельно не измерен.

Счётчик также показывает только 69 rotation-вызовов на 1648 alg_unquant
при 192 кбит/с. ASM-only устранение повторного celt_rcp здесь возможно
проверить отдельно от отвергнутого C-варианта, но по частоте это не первый
кандидат для 192 кбит/с. Частые entropy operations остаются приоритетнее.
[Полный host-профиль с хешами](../firmware/development/esp8266-opus-bands-fused-v1/host-shapes.json).

## Физический результат: не принимать

10 контролей, 10 fused, 10 tell-intensity, 10 повторных контролей; две
контрольные серии общие. Все 40 попыток сохранены, все PCM hashes/samples/
packets и scratch совпали. В таблице медианы CPU-бюджета всего raw-декодера.

| кбит/с | Контроль до | Fused | Контроль после | Изменение времени к первому контролю |
|---|---:|---:|---:|---:|
| Mono 12 | 22,776% | 22,745% | 22,780% | −0,14% |
| Mono 24 | 54,698% | 54,415% | 54,704% | −0,52% |
| Stereo 64 | 65,532% | 67,694% | 65,569% | +3,30% |
| Stereo 128 | 80,731% | 82,904% | 80,720% | +2,69% |
| Stereo 192 | 92,898% | 97,088% | 92,853% | +4,51% |

App 903120 B (+128); flash text +124 B, rodata +4 B. Новых codec tables нет.
DRAM data/bss 1652/18752 B, IRAM vectors/text/bss 128/22900/4044 B,
audio stack 5120 B и lifetime free 1660 B — как у контроля.
Минимальная наблюдённая DRAM: первоначальный контроль 8168 B, fused 8176 B,
tell-intensity 7152 B, повторный контроль 1184 B. Это динамические значения,
не экономия/рост статической памяти кодека. Максимальный wall-вызов fused
на 192 кбит/с 24284 мкс, не гарантия deadline 20 мс.

В повторном контроле run4 сохранены HTTP observation timeout и выброс
105,571% CPU на 192; run3/4 имели низкий запас DRAM. Финальные результаты
получены, попытки не выброшены и не заменены. Причина выброса/падения heap
не установлена. Этот тест не квалифицирует WebUI или непрерывный звук.
Обычная I2S PDM-прошивка возвращена через OTA, слот 0x10000, stopped.
После возврата отдельный GET главной страницы дал HTTP 200 за 121 мс;
status: Wi-Fi подключён, error пуст, свободно 27452 B. Это один smoke-запрос,
не браузерная загрузка всех ресурсов и не проверка непрерывности аудио.
Итоговые 23 регрессионных теста прошли, без skip/fail.

[Все данные и проверка выбора](../firmware/development/esp8266-opus-bands-fused-v1/comparison.json).
Лучший ранее измеренный backend остаётся tell-inline: 88,136% CPU;
цель ≤70% и live-квалификация ещё не достигнуты.
