# Opus ASM: указатель many-dimensions PVQ

Дата: 2026-09-14. Контроль — принятый PVQ row-loop, не micro-bundle.

## Изменение

В `decode_pulses/cwrsi` развёрнутый поиск `ROW[k-j][n]` повторял SLLI+ADD
для каждой пробы. Теперь свободный a0 удерживает адрес элемента ROW:
первая проба вычисляет его как прежде, следующие уменьшают на4.
Нулевая координата и ветвь many-pulses не изменяются.

Адрес возврата сохранён в private sp+44 по0x402531cd, единственный CALL0
завершён до изменяемого участка; a0 больше не читается до восстановления
по0x40253584. Проверяются все переходы и RET, frame остаётся48 байт.
На прежнем общем выходе все регистры, кроме мёртвого a0, совпадают;
SAR, последовательность чтений, знаки, index/K, последующие PCM/yy сохранены.

Один frozen диапазон0x402533b9..0x402534d7,286 байт:116→103 инструкции,
261 исполняемый байт и25 недостижимых после безусловного J. Общий MOV
оставлен по0x402534d5: первый успешный проход не получает лишнего перехода.
Проба8 использует BLTU/J из-за дальности условного перехода; только выход
на ней требует одной дополнительной команды относительно обычной экономии.
Для выхода после j проб экономия j−1 команд (j−2 при j=8), максимум14.
Это не оценка тактов: flash/cache и зависимости могут изменить эффект.

Не введено нового ограничения K/N/битрейта:15 выходов — структура исходного
linked блока. Доказательство охватывает произвольные uint32 значения и все
его пути, а не только индекс/битрейт тестовых записей. Читаются те же ячейки,
лишнего prefetch или новой таблицы/буфера нет. Исходные C и GCC ASM не менялись.

## Проверки

- [x] Символьное сравнение всех15 выходов, регистров, адресов чтения и условий.
- [x] 6720 numeric linked случаев: uint32 wrap, знаки, равенства, границы и
      длинные поиски; каждая лишняя память вне выбранного пути вызывает ошибку.
- [x] Host:24 exact PCM/ASan/UBSan/PLC/reset/OOM сценария до510 кбит/с.
- [x] 66 связанных регрессий PASS,0 FAIL/skip; отрицательные проверки
      ловят неверный decrement/чтение/условие, interior entry и живой a0.
- [x] Физические10 A +10 B +10 A при160 МГц/QIO40 и прежних RAM/стеке.
- [x] Все попытки/максимумы/память сохранены; оба high-bitrate gate FAIL.
- [x] Ordinary восстановлена OTA; HTTP/WS/станция/плейлист проверены.
- [x] Итоговые68 связанных регрессий PASS,0 FAIL/skip, включая независимый
      пересчёт всех30 физических попыток и проверку восстановления платы.

Host-модель заменяет только второй поиск в C на pointer decrement;
она не исполняет LX106 ASM и не измеряет физическую скорость. Исполняемые
инструкции отдельно проверяются по linked образу. Предыдущие отрицательные
опыты не включены в контроль; проверяется новая независимая гипотеза.
По census на192 таких дополнительных проб около1254/с: ожидаемый эффект мал,
но точный кандидат можно сохранить для комбинаций, если он неразличим отдельно.

## Воспроизведение

`node tools/esp8266_opus_asm/pvq_dim_pointer.cjs`

`node tools/esp8266_opus_asm/check_bands.cjs pvq-dim-pointer`

`node --test tests/esp8266-opus-pvq-dim-pointer.test.js`

`node tools/esp8266_opus_asm/report_pvq_dim_pointer.cjs` — после30 запусков.

Образы: `firmware/development/esp8266-opus-pvq-dim-pointer-{control,candidate}-v1/`.
903216 байт каждый, static RAM/stack delta0. C fallback/default прежние.
Control SHA: `9069d279220b60c7da9b8615fa0d47e54e9e5ace2428ac66ba1a00300ad87164`.
Candidate SHA: `264ae5288a6324d4d3e552cd0600f63484b7e363dd00409eee91e8706b6737ac`.
Raw тест: пакеты заранее в RAM, без аудиосети/вывода/function/stage profiler;
Wi-Fi/WebUI включены. Скорость измерена, ускорение и цель80% не подтверждены.

## Физический результат: отдельно не принят

Все30 попыток завершены с точным PCM/hash. Ошибок HTTP observations и
превышений task-window над wall-window не было; ничего не вычиталось и
не фильтровалось. A/B/A последовательный, не interleaved. Task CPU включает
начисляемое ISR/служебное время измерения; это не время полного live-плеера.

| кбит/с | CPU A, % | CPU B, % | CPU A2, % | Максимальный вызов A/B/A2, мкс |
|---|---:|---:|---:|---:|
|12|23.08204|23.09273|23.08938|7850 /7810 /7596|
|24|54.31444|54.32848|54.31423|14740 /14836 /14288|
|64|64.67077|64.64585|64.61279|16893 /17527 /17407|
|128|76.63825|76.65156|76.62279|19670 /22762 /19928|
|192|87.31173|87.31810|87.33048|24604 /22150 /21166|

На192 относительно A замедление0.00730%, относительно A2 выигрыш0.01417%.
На128 замедление0.01737% /0.03755%. Оба high-bitrate gate FAIL; устойчивого
ускорения не видно. Разброс CPU192: A87.25929–87.40371%,
B87.27250–87.37362%, A2 87.27171–87.37246%. Столь малые различия сами
по себе не доказывают вредность этой микроправки: она остаётся в пуле
для других обоснованных комбинаций, но не становится новым baseline/default.

Наблюдаемая минимальная свободная DRAM A/B/A2 —7668 /8176 /8528 байт;
минимум свободного стека1660 байт во всех режимах. Это свободная память,
не размер декодера. Static RAM/frame/scratch относительно контроля не выросли.
Все исходные JSON/log, SHA и пересчитанные распределения:
[comparison.json](../firmware/development/esp8266-opus-pvq-dim-pointer-candidate-v1/comparison.json).

По [сохранённому census](benchmarks/esp8266-opus-pvq-path-profile-2026-09-14/summary.json)
на192 кбит/с:1593 поиска,301 дополнительная проба,0 выходов на пробе8
за0.24 секунды. Для проверенной последовательности это301 удалённая
инструкция на фрагмент (около1254/с), не оценка сэкономленных тактов.
Редкость пути ограничивает ожидаемый общий эффект; многократное повторение
той же микроправки без новой комбинации/гипотезы не имеет приоритета.

## Восстановление платы

Обычная live512-idle3s восстановлена OTA в0x10000, SHA
`661becd301b07885d493ceb1b513d9e874b7d86e4ada8231da8657aa90983c4b`.
HTTP200 за108.9244мс,27249 байт gzip: ответ на основной документ,
не полная браузерная загрузка. WS/getindex/current167, stopped и SHA
плейлиста совпадают с начальным состоянием; error пусто. RSSI−55,
free heap27448/min24748, web stack2324 байт. Аудио не запускалось при
восстановлении. Непрерывные20 секунд I2S и цель80% этим опытом не подтверждены.
