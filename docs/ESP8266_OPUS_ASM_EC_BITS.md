# Opus ASM: извлечение low bits через high в ec_dec_bits

2026-09-14. Отдельный frozen-layout кандидат поверх принятого PVQ row-loop.
Лучший raw CPU192 около87.3%, текущая цель80% ещё не достигнута.

## Изменение и точность

Сохранённый GCC ASM строит `(1U<<bits)-1` через MOVI−1/SSL/SLL/XOR/AND.
Одновременно для обновления `end_window` вычисляется `high=window>>bits`.
Кандидат использует `low=window^(high<<bits)`. В общем хвосте
0x40298575..0x40298598 это14→13 инструкций,35→33 исполняемых байта.
Остальные2 байта — недостижимое заполнение после RET, не выполняемые NOP.

Полностью прежние байты вне одного35-byte диапазона, адрес функции/литералов,
refill/EOF, порядок обращений к entropy state, стек и RAM. Leaf call0 ABI:
сохранены return/a1/a12..a15, выход a2 и финальный SAR. Только a5/a9 могут
отличаться как caller-saved scratch. В том числе восстановление SAR учтено
в13 инструкциях: экономия не завышена за счёт изменения контракта.

Документированный `entdec.h` допускает bits0..25; побитовая символьная
проверка покрывает0..31, произвольные32 бита window/state. Это не новый
лимит битрейта. Численная проверка готовых linked инструкций покрывает81120
состояний всей функции,32448 refill и20280 EOF; счётчик nbits_total проверен
с wraparound. C fallback и исходный GCC snapshot неизменны.

## Проверки

- [x] Два образа903216 B, RAM/frame без роста, XOR/SHA SDK image корректны.
- [x]32 символьных shift cases и81120 численных full-function cases точны.
- [x]24 host-сценария exact PCM/guards/reset/OOM/PLC, до510кбит/с,
  фазы/2.5–20мс, составные120мс/48 frames. Host — semantic mirror, не Xtensa.
- [x]63 связанных preflight-регрессии PASS/0skip, включая negative mutations
  операций/порядка памяти/SAR и реальные поля памяти host mono/stereo.
- [x]10 A/10 B/10 A на плате, без function/stage profiler, вход в RAM.
- [x] Проверить high-bitrate gate, текущий порог80%, все max/RAM/выбросы.
- [x] Вернуть обычное радио OTA, проверить HTTP/WS/станцию/плейлист.
- [x]65 итоговых связанных ASM-регрессий PASS/0skip, включая физический
  отчёт и независимый пересчёт CPU/max/RAM. Это не все тесты репозитория.

Артефакты: `firmware/development/esp8266-opus-ec-bits-{control,candidate}-v1`.
Candidate SHA256 `fa2bf5e6afb71b7184454b738e1027664852b03e13959560732b576f029b317a`.
Профиль CPU160/QIO40, no output/function/stage profiling; default не меняется.
Уменьшение числа инструкций само по себе не считается ускорением.

```powershell
node tools/esp8266_opus_asm/ec_bits.cjs
node tools/esp8266_opus_asm/check_bands.cjs ec-bits
node --test tests/esp8266-opus-ec-bits.test.js
```

## Физический результат: не принят

Все30 попыток завершены, PCM точен, ошибок decoder/HTTP observations нет.
Это raw RAM benchmark при включённых Wi-Fi/WebUI, без аудиосети/вывода
и function/stage profiler. Task CPU включает начисленное ISR и служебное
измерение; A/B/A последовательный, не interleaved. Ничего не вычиталось.

| кбит/с | CPU A, % | CPU B, % | CPU A2, % | Максимальный вызов A/B/A2, мкс |
|---|---:|---:|---:|---:|
|12|23.08606|23.08948|23.09119|7583 /7766 /7316|
|24|54.32360|54.33769|54.31848|15996 /16493 /14276|
|64|64.65450|64.65758|64.62846|17561 /17615 /17554|
|128|76.62877|76.66525|76.63775|19408 /21918 /19782|
|192|87.35529|87.32860|87.33156|23057 /24502 /23458|

На192 относительный выигрыш0.03055% относительно A и только0.00339%
относительно A2:5.34 и0.59мкс на20мс аудио. На128, напротив, время выросло
на0.04761% /0.03588%. Оба high-bitrate gate FAIL; это не устойчивый полезный
выигрыш полного декодера. Максимальный вызов B/192 —24.502мс, хуже контроля.
Максимальные CPU192:87.39342 /87.41679 /87.38200%; порог80% не пройден.

RAM/frame/арены не менялись. Минимальная наблюдаемая DRAM A/B/A2:
8352 /8160 /8176 B; свободный стек минимум1660 B во всех группах.
Медианы DRAM после запуска26648 /26476 /26476 B. Сохранён A2/run4/mono12:
task window превышает wall на341мкс. Окна различаются, выброс не фильтровался.
Все JSON/log, SHA и распределения находятся в
[comparison.json](../firmware/development/esp8266-opus-ec-bits-candidate-v1/comparison.json).

Обычная live512-idle3s восстановлена через OTA в0x10000, SHA
`661becd301b07885d493ceb1b513d9e874b7d86e4ada8231da8657aa90983c4b`.
HTTP200 за101.4959мс,27249 B gzip (не полная браузерная загрузка).
WS/getindex/playlist SHA и stopped station167 прежние; error пусто,
RSSI−47, heap27448 B/min24748 B, web stack2324 B. Audio при восстановлении
не запускалось. Это не20-секундная live-квалификация экспериментального ASM.

Default/C fallback прежние; кандидат сохранён для воспроизводимости.
Лучший ASM остаётся accepted PVQ row-loop около87.3%, цель80% не достигнута.
Следующая ещё не проверенная гипотеза из opportunity list — reuse адресов
ROW[k-j] в many-dimensions PVQ. Не повторять уже принятый many-pulses
pointer-loop как новый опыт; по census этот следующий путь существенно реже.

## Уточнение статуса для комбинаций, 2026-09-14

По уточнению пользователя малые ASM-изменения нельзя исключать только из-за
неразличимого отдельного эффекта: выигрыш может проявиться в комбинации.
Этот вариант 14→13 инструкций сохраняется как кандидат для таких проверок,
после проверки совместимости с остальными overlays. Оба исторических speed
gate остаются FAIL: самостоятельное ускорение не подтверждено, принятый
baseline и default не меняются. Измерения выше, включая ухудшение на128
и максимальный вызов, не удаляются и не переименовываются в PASS.

Критерии точности, RAM и скорости применяются ко всему проверяемому пакету
по [обновлённому плану](ESP8266_OPUS_ASM_BANDS_OPTIMIZATION_PLAN.md#накопление-микроправок-и-проверка-комбинаций).
