# Opus ASM: точный двоичный поиск PVQ

2026-09-19. Эксперимент поверх accepted eBands-final, не новый production default.

## Изменение

Только строковый поиск `decode_pulses` при K >= N > 2 и U(N,N) <= index.
Линейный проход от K вниз заменён двоичным поиском максимального j,
для которого U(N,j) <= index. Используются прежние таблицы flash и регистры.
Никакого ограничения битрейта, нового буфера, LUT или heap allocation нет.
Колонный поиск, энтропийное декодирование, знак, энергия и нормализация прежние.

ASM: `tools/esp8266_opus_asm/pvq_binary.s`. C fallback и сохранённый GCC ASM
не меняются; отдельная C-модель нужна только для host-регрессий.

## Инварианты и размещение

- lo=N, hi=K+1, p=U(N,N). Mid всегда строго внутри интервала; hi не читается.
- При U(N,mid)>index сдвигается hi, иначе lo и сохранённый p. Интервал строго
  уменьшается. При hi-lo=1 получаются прежние K и p; остальные вычисления те же.
- Патч поиска: 0x40253329..0x40253354, 43 байта. CALL0 во вспомогательный leaf.
- Leaf: 0x40251024..0x40251082, 94 байта прежнего encoder-only участка
  quant_all_bands. Требуется проверенный native caller encode=0 и аудит всех
  ссылок/алиасов, а не вывод по текущим тестовым потокам.
- a0 уже сохранён исходным decode_pulses на SP+44 и не читается до восстановления.
  a3 перезаписывается общей ветвью до чтения. Остальные живые регистры точны.
- Исходный frame 48 байт, ни одной новой записи в память или SAR.
- Внешние адреса функций, размеры секций, данные и все байты вне двух патчей
  сохраняются. Код для других процессоров остаётся исходным C.

## Проверки и критерий принятия

- Host: 24 сценария PCM/state/ASan/UBSan, включая 320/510 кбит/с, PLC/error
  self-test, смену потоков, разные длительности кадров и составные пакеты.
- Linked proof: реальные U-строки из ELF, каждый интервал значений с границами
  и серединой; unsigned-сравнения, ограничения чтения и выходные регистры.
  Последняя строка N=14 содержит только диагональ, без допустимой пары K,K+1.
- Отрицательные тесты неверного midpoint, направления ветви, ширины чтения,
  кэша p, ABI и повреждённой таблицы.
- После полного preflight: свежие 10A/10B/10A2 на плате, raw CPU160/QIO40,
  пакеты в RAM, без вывода и function/stage profiler. Сохранять все попытки.
- Выигрыш 128/192 должен перевесить относительную потерю низких битрейтов
  в обоих сравнениях. Уменьшение числа чтений не считается CPU-ускорением.

Предварительные проверки пройдены: 303906 случаев linked-интерпретатора,
24 host PCM/state/sanitizer сценария, пять регрессий с отрицательными мутациями.
App 903216 байт; static RAM и frame не увеличены. Полная цепочка предыдущих
accepted ASM-патчей перепроверена перед публикацией кандидата.

## Физический результат: отклонён

Завершены свежие10A/10B/10A2, все30 попыток сохранены и все PCM точны.
Медианы raw CPU, пакеты в RAM, без физического вывода и stage/function profiler:

| Поток | Контроль A | Binary B | Повтор A2 |
| --- | ---: | ---: | ---: |
| 12 кбит/с |23.087521%|23.095208%|23.087042%|
| 24 кбит/с |53.893917%|53.943125%|53.893333%|
| 64 кбит/с |61.204354%|61.371438%|61.202875%|
| 128 кбит/с |70.387604%|70.802042%|70.376375%|
| 192 кбит/с |77.872438%|78.451979%|77.871979%|

На192 относительное замедление0.7442/0.7448%; оба speed gate FAIL.
Остаётся accepted eBands-final; новый binary не переносится в production.

MinDRAM по группам756/196/1928B; stack-free минимум1660B в каждой группе.
Максимальный вызов192:27986/19852/20323us. A/run1 содержит HTTP timeout
наблюдения и90.004750% CPU; попытка не исключена и не заменена.
Новый код не добавляет static RAM/frame/scratch; малые heap minima сохранены,
но не являются доказательством причины сетевых колебаний.

34 локальных регрессии PASS, включая пересчёт всех попыток и подтверждение
OTA-слотов. Обычная `firmware/development/esp8266-main/app.bin` восстановлена
через OTA; HTTP, WS getindex и playlist отвечают, радио оставлено остановленным.
Физическое непрерывное воспроизведение этим опытом не проверяется. Цели75%
raw CPU и20s I2S PDM/WebUI не достигнуты.

Все доказательства: `firmware/development/esp8266-opus-pvq-binary-candidate-v1/`.
Повтор отчёта: `node tools/esp8266_opus_asm/report_pvq_binary.cjs`.

## Почему меньше чтений ещё не означает быстрее

`analyze_pvq_binary_instructions.cjs` воспроизводит сохранённые реальные
PVQ-поиски через оба linked instruction stream. Во всех поисках берётся
нижняя граница известного интервала ранга: внутри этого интервала результаты
всех сравнений неизменны. Это число инструкций, **не число тактов**.

| Поток | Инструкции прежнего поиска | Binary | Чтения U: прежний → binary |
| --- | ---: | ---: | ---: |
| 64 кбит/с | 14681 | 33898 | 2803 → 2379 |
| 128 кбит/с | 60732 | 134966 | 11682 → 9594 |
| 192 кбит/с | 119961 | 237196 | 24259 → 17587 |
| 320 кбит/с, 20 мс | 292130 | 326364 | 66093 → 25785 |
| 510 кбит/с | 1644283 | 1645117 | 377680 → 132249 |

На192 лишь263 из5205 поисков имеют меньше инструкций,4918 — больше.
Около65% результатов находятся в первых четырёх пробах линейного поиска.
Расчёт midpoint, границ, кэширование p и вызов helper перевешивают экономию
загрузок в этих коротких случаях. Цена flash/cache дополнительно зависит
от платы; по числу инструкций нельзя доказать конкретный процент замедления.

Следующая независимая гипотеза: сохранить короткий линейный fast path и
проверить частичное разворачивание его тела с общим обновлением указателя/K.
Перед новой прошивкой считать динамические инструкции на этих же трассах;
универсальный binary при каждом входе в row path не повторять без новой причины.
