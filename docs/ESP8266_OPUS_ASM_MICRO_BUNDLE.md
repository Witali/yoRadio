# Opus ASM: пакет трёх микроправок

Дата: 2026-09-14. Ветка: `codex/esp8266-opus-asm`.

По уточнению пользователя проверяется накопительный эффект малых изменений,
а не необходимость измеримого выигрыша каждого компонента по отдельности.
Контроль — принятый PVQ row-loop поверх MDCT/tell-inline; никакой новой
перелинковки остальных функций, RAM, стеков или таблиц.

## Состав

1. `ec_dec_bits`: 14→13 инструкций хвоста, low = window XOR (high << bits).
   Сохранены SAR, refill/EOF, entropy state; 35 байт, из них 2 после RET.
2. `opus_fft_impl`: 4→3 загрузки, исключено повторное чтение private sp+32.
   Прежние 8 байт и порядок чтения complex r/i; исполняемого padding нет.
3. `clt_mdct_backward_c`: 16 MULL→MUL16S, только при доказанной знаковой
   ширине обоих операндов ≤16. Полный int32 результат точен, unsigned low16
   не заменяются. Анализ CFG заново выполнен на текущем post-pair layout:
   восемь адресов отличаются от отдельного старого опыта MUL16S.

Это 18 непересекающихся диапазонов, 91 байт области замен. Все остальные
байты ELF и flash-образа неизменны (кроме контрольных сумм образа).
Компоненты не считаются уже принятыми только из-за меньшего числа инструкций.
Старые отдельные FAIL не переписываются; обычная прошивка и C fallback прежние.

## Корректность и воспроизведение

- [x] Зафиксировать recipe, ASM-комментарии, исходные зависимости и SHA.
- [x] Проверить живые регистры, ABI/SAR, несовместимые пересечения и старые адреса.
- [x] Entropy: 32 symbolic shift counts, 81120 linked numeric случаев с refill/EOF.
- [x] FFT: символьное равенство конечных регистров и порядка data reads.
- [x] MDCT: полный signed-width CFG и 589824 численных edge-проверки.
- [x] Host: 24 exact PCM/ASan/UBSan/PLC/reset/OOM сценария до 510 кбит/с.
- [x] 60 связанных регрессий PASS, 0 FAIL/skip; лог сохранён с артефактами.
- [x] Физические 10 A + 10 B + 10 A; все попытки/ошибки/максимумы/RAM сохранены.
- [x] Проверить high-bitrate gate и цель ≤80% CPU192: оба условия не выполнены.
- [x] Вернуть обычное радио OTA, проверить HTTP/WS/станцию/плейлист.
- [x] Итоговые 62 связанные регрессии PASS, 0 FAIL/skip, включая независимый
      пересчёт 30 физических попыток и проверку восстановления платы.

Host изменяет только выражение entropy: FFT-read reuse и signed16 multiply
тождественны исходной арифметике, их проверяют linked ASM-доказательства.
Host не исполняет LX106 ASM и не измеряет его скорость. Физический raw тест:
пакеты заранее в RAM, 160 МГц/QIO40, без аудиовывода/сетевого чтения аудио и
function/stage profiler; Wi-Fi/WebUI остаются включены. Это не live-квалификация.

Рецепт: `node tools/esp8266_opus_asm/micro_bundle.cjs`.
Host: `node tools/esp8266_opus_asm/check_bands.cjs micro-bundle`.
Проверки: `node --test tests/esp8266-opus-micro-bundle.test.js`.
Отчёт: `node tools/esp8266_opus_asm/report_micro_bundle.cjs` (после 30 запусков).

Образы: `firmware/development/esp8266-opus-micro-bundle-{control,candidate}-v1/`.
Оба 903216 байт; изменение статической RAM/стека — 0.
Control SHA: `9069d279220b60c7da9b8615fa0d47e54e9e5ace2428ac66ba1a00300ad87164`.
Candidate SHA: `a19234f89e9f95da1247a0f6f4cfd8da194f000f1731ee58f2cab11d6992f07c`.
Физическая скорость измерена; устойчивое ускорение не подтверждено.
Цель остаётся открытой.

## Физический результат: комбинация не принята

Все 30 попыток завершены, PCM/hash и состояния точны. Ошибок наблюдения HTTP
и превышений task-window над wall-window в этой серии не было. A/B/A
последовательный, не interleaved; никакие выбросы или ISR-время не вычитались.

| кбит/с | CPU A, % | CPU B, % | CPU A2, % | Максимальный вызов A/B/A2, мкс |
|---|---:|---:|---:|---:|
|12|23.08885|23.09344|23.08877|8444 /8635 /7185|
|24|54.32088|54.32306|54.32154|15280 /15127 /14246|
|64|64.67865|64.65865|64.61727|17107 /17270 /17448|
|128|76.64027|76.66813|76.62838|19779 /20173 /19427|
|192|87.32385|87.31594|87.31394|22059 /21951 /22413|

На192 относительно A выигрыш 0.00907%, но относительно A2 замедление 0.00229%:
это экономия примерно 1.58 мкс либо потеря 0.40 мкс на 20 мс аудио.
На128 замедление 0.03634% /0.05187%. Оба high-bitrate gate FAIL; устойчивого
общего ускорения нет. Разброс CPU192: A87.29075–87.39754%,
B87.26688–87.38167%, A2 87.26988–87.33458%. По одним таким малым различиям
не объявляем компоненты доказанно вредными и не удаляем их из пула.

Статическая RAM, frame и арены не увеличены. Минимальная наблюдаемая DRAM
A/B/A2 —8184 /8212 /8352 байт; свободный стек минимум1660 байт во всех режимах.
Это минимумы свободной памяти, а не расход декодера. Все JSON/log и SHA:
[comparison.json](../firmware/development/esp8266-opus-micro-bundle-candidate-v1/comparison.json).

Сохранён факт: эта конкретная комбинация не даёт воспроизводимого выигрыша;
нельзя обещать арифметическую сумму эффектов. Компоненты остаются отдельными
кандидатами для других обоснованных сочетаний. Контроль/baseline остаётся
принятый PVQ row-loop; default и C fallback не меняются. Следующая независимая
гипотеза плана — reuse ROW[k-j] в many-dimensions PVQ, не повтор уже принятого
many-pulses row-loop. У неё существенно меньше частота вызовов по census.

## Восстановление платы

Обычная live512-idle3s восстановлена OTA в0x10000, SHA
`661becd301b07885d493ceb1b513d9e874b7d86e4ada8231da8657aa90983c4b`.
HTTP200 за109.5525мс,27249 байт gzip — это ответ на основной документ,
не полное время открытия страницы в браузере. WS/getindex и SHA плейлиста
проверены; станция167/stopped прежние, error пусто. RSSI−56, free heap27448,
minimum24748, web stack2324 байт. Воспроизведение при восстановлении не
запускалось. Этот опыт не подтверждает непрерывный live I2S или достижение80%.
