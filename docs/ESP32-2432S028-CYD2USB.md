# ESP32-2432S028 CYD2USB

This fork is configured for the two-USB `ESP32-2432S028` revision with an
ST7789 display. The hardware profile is in `yoRadio/myoptions.h` and is picked
up automatically by the sketch.

## Connected hardware

| Function | ESP32 pins |
| --- | --- |
| ST7789 display (HSPI) | SCK 14, MOSI 13, MISO 12, CS 15, DC 2, reset tied to EN |
| Display backlight | GPIO21, active high PWM |
| XPT2046 touch (VSPI with custom pins) | SCK 25, MOSI 32, MISO 39, CS 33 |
| Onboard mono amplifier | internal DAC2 on GPIO26 |
| microSD socket | SCK 18, MOSI 23, MISO 19, CS 5 (disabled) |

The display occupies HSPI. Touch uses VSPI remapped to its dedicated pins.
Although the microSD socket is also wired to ESP32 pins, it cannot be enabled
at the same time by the current yoRadio SPI abstraction: the board presents
display, touch, and microSD as three independently wired SPI buses while the
ESP32 exposes only two general-purpose hardware SPI controllers. Internet
radio, the display, touch controls, brightness control, and onboard sound are
enabled.

## Arduino build

Select **ESP32 Dev Module**, **4MB (32Mb)** flash, and **Minimal SPIFFS
(1.9MB APP with OTA/128KB SPIFFS)**. The default partition scheme has only a
1.2MB application slot and is too small for the color-display build. The
128KB SPIFFS partition is sufficient for the current 29KB of web assets.

Use an ESP32 Arduino core supported by upstream yoRadio. Install the libraries
listed in the upstream README, including:

- Adafruit GFX Library
- Adafruit ST7735 and ST7789 Library
- XPT2046_Touchscreen
- RTClib

Then compile the `yoRadio/yoRadio.ino` sketch. Upload the sketch data from
`yoRadio/data` as described by upstream before first use.

Equivalent Arduino CLI board selection:

```text
esp32:esp32:esp32:FlashSize=4M,PartitionScheme=min_spiffs,PSRAM=disabled
```

The internal DAC channel is explicitly limited to DAC2/GPIO26. Enabling both
internal DAC channels would drive GPIO25 and break the touch clock.

## Touch calibration

The profile uses yoRadio's default XPT2046 calibration. Enable touch debugging
in the web UI if the edges do not line up on a particular panel batch, then set
`TS_X_MIN`, `TS_X_MAX`, `TS_Y_MIN`, and `TS_Y_MAX` in `yoRadio/myoptions.h`.
