# Opus ASM: возможности оптимизации после полного статического обзора

Дата: 2026-09-14. ESP8266 / Xtensa LX106, CPU 160 МГц.

Дополнение к [основному плану](ESP8266_OPUS_ASM_BANDS_OPTIMIZATION_PLAN.md).
Это список кандидатов, а не подтверждённых ускорений и не изменение default.
Просмотрены 110 единиц сохранённого GCC ASM, содержащих 229 функций;
горячие фрагменты дополнительно сверены с linked ELF лучшего tell-inline.
Наличие функции в ELF не доказывает её выполнение на конкретном потоке.

Цель остаётся прежней: медиана raw CPU не более 70% на Opus 192 кбит/с,
побитно идентичный PCM, отсутствие роста RAM, затем непрерывный I2S PDM
не менее 20 секунд с работоспособным WebUI. 192 кбит/с — цель измерения,
не ограничение допустимого битрейта.

## 1. Повторные чтения указателей и значений со стека

- [ ] Заменить повторные загрузки в quant_band, quant_partition,
      quant_all_bands и opus_fft_impl использованием уже загруженного регистра.
- [ ] Для каждого места доказать отсутствие изменения памяти, регистра-источника
      и альтернативного входа в середину заменяемого фрагмента.
- [ ] Проверить фактические инструкции после линковки, живые регистры и ABI.

Конкретный пример из gcc/upstream/celt/kiss_fft.c.s, opus_fft_impl,
чтение scratch[0] из Fout0 (исходная строка 1565):

```asm
# Сейчас: два чтения одного указателя из private stack.
l32i.n a2, a1, 32
l32i.n a3, a1, 32
l32i.n a2, a2, 0
l32i.n a3, a3, 4

# Кандидат: тот же результат a2/a3, без дополнительного регистра.
l32i.n a3, a1, 32
l32i.n a2, a3, 0
l32i.n a3, a3, 4
```

В quant_band аналогичны два чтения ctx из sp+56 перед чтением encode
и tf_change: bands-tell-inline/upstream/celt/bands.c.s, строка 1773.
В случаях, где обе копии нужны одновременно, повторный L32I можно заменить
MOV, а не удалять назначение регистра.

Первый независимый эксперимент: [frozen reloads](ESP8266_OPUS_ASM_FROZEN_RELOADS.md).
Собран кандидат с восемью заменами load на MOV.N/OR, без изменения размеров
инструкций, адресов и RAM. Symbolic/linked proof проходит. Все30 физических
A/B/A завершены, PCM точен: CPU192 88.17585 /88.13479 /88.13423%; ускорение
не подтверждено, вариант OFF. На128 также нет выигрыша.19 итоговых тестов
PASS, обычное радио возвращено OTA. Исходный пример 4→3 инструкции этим
опытом не реализован: сначала изолирована цена повторного memory load.

Строки относятся к текущему snapshot; рецепт должен привязываться к hash
и проверяемой последовательности, а не слепо к номеру строки. Макросы,
в частности Y_OPUS_TELL_FRAC, тоже меняют регистры: нельзя считать два
окружающих их L32I дубликатами только по тексту исходного ASM.

## 2. Парное чтение таблиц MDCT

- [ ] Для соседних 16-битных коэффициентов и окна использовать
      один выровненный L32I с извлечением двух половин.
- [x] Отдельно проверить пары bitrev в свободном a0:30 A/B/A, точность
      подтверждена, но самостоятельный вариант не прошёл повторный speed gate.
- [ ] Выбор начальной половины слова и неизменные смещения определять
      до цикла; нечётные размеры и хвост обрабатывать явно.
- [x] Сначала отдельно проверить более локальное упрощение выбора половины:
      убрать лишние маски/переходы в текущем table_read16 ASM.
- [ ] Проверить все размеры transform, знаковое расширение, границы таблиц
      и точный порядок in-place post-rotation, особенно центральную пару.

Место: gcc/upstream/celt/mdct.c.s, clt_mdct_backward_c, цикл .L37
(строка 907), далее post-rotation и TDAC. Сейчас выбор половины включает
повторные проверки бита адреса и местами J на следующую инструкцию.
Парная обработка требует живых регистров, но не нового массива RAM.
Не читать за границей таблицы ради последней пары и не заменять flash/IRAM
L32I короткими L16SI/L16UI.

Локальная проверка завершена: [MDCT half](ESP8266_OPUS_ASM_MDCT_HALF.md), пять
замен17 B при прежнем размещении.30 A/B/A, CPU192 88.11908 /88.06342 /88.10215%.
Очень небольшой выигрыш0.044–0.063% времени; оба сравнения проходят критерий
128/192 против замедления mono12. Сохранён экспериментально, не default.
Максимальный вызов не улучшился:23.009мс у кандидата.18 регрессий/точный PCM,
RAM/stack прежние, ordinary восстановлен OTA/HTTP/WS. Этот опыт сохраняет
все исходные L32I и меняет только выбор половины.

Следом завершён [bitrev pair](ESP8266_OPUS_ASM_MDCT_BITREV_PAIR.md): одна24-byte
замена, пары int16 удерживаются в a0 без нового RAM/stack.30 A/B/A:
CPU192 88.05546 /88.02013 /88.07158%; PCM точен,24 теста PASS/0skip.
Отдельный кандидат **не принят**: против A2 выигрыш128 всего0.01595%, меньше
замедления12 0.02031%. Максимум на192 кбит/с —22.621мс, также не лучше контроля.
Ordinary восстановлен OTA/HTTP/WS, все попытки сохранены. Не засчитывать
этот опыт как выполненное парное чтение trig/window или как новый accepted best.

### Следующие варианты после отдельного опыта bitrev

- [x] Переставить арифметику pre-rotation в исходном ASM: вычислять `yr`
      последовательно, используя аккумулятор a7 и временный a11, затем `yi`.
      a14/a15 освобождены без новых spill; скорость проверяется отдельно.
- [x] Если регистры свободны, удерживать в a14/a15 пары `t0/t1`, а в a0 —
      `bitrev`. При чётном индексе читать три выровненных слова, при нечётном
      брать их старшие половины. Проверять фазу и границы всех таблиц по ELF.
      Не переносить этот fast path на произвольный custom mode без fallback.
- [x] Использовать одну проверку чётности только после доказательства, что
      все три указателя имеют одинаковую фазу. Адрес `t0` и отрицательное
      смещение `2*N4` вычислять только на итерациях, действительно читающих слово.
- [x] Сохранить порядок чтения PCM/записи `yp`, значения аргументов FFT и
      используемые после цикла регистры. `a15` при выходе должен снова содержать
      `yp`; оригинальный return a0 остаётся в sp+92. Проверить liveness через CALL0.
- [x] Доказать точный модульный32-битный результат каждой `S_MUL` и их сумм.
      Ассоциативность ADD32 разрешает перестановку сумм, но не перенос SRAI
      через сложение или объединение округлений двух произведений.
- [x] По возможности уместить весь новый pre-rotation в прежний диапазон
      с недостижимым padding после перехода: остальные функции/таблицы должны
      остаться на прежних адресах. Изменение числа загрузок и spill измерять.

Реализован [three-table pair](ESP8266_OPUS_ASM_MDCT_THREE_PAIR.md): один193-byte
диапазон, все остальные байты ELF прежние.450 symbolic пар и16 полных
численных transform проходят; RAM/stack прежние. Завершены30 A/B/A:
CPU192: 88.05410 /87.68704 /88.06704%; оба high-bitrate gate проходят.
23 итоговых регрессии PASS, PCM точен, все максимумы сохранены. Кандидат
принят как экспериментальный raw baseline; ordinary восстановлен OTA/HTTP/WS.
70% и live I2S ещё не достигнуты. Следующий отдельный опыт — MUL16S ниже.

### Следующая гипотеза: четыре пары post-rotation

- [x] Отдельно от MUL16S переработать только post-rotation диапазон
      0x402473a0..0x402474b4 (276 B) принятого three-table ELF.
      Не менять MULL, окно TDAC или соседние функции/адреса в этом опыте.
- [x] Переставить суммы двух комплексных вращений: первый yr на a10/a13,
      второй yr на a7/a13. Это может освободить a14 без spill, как в pre-rotation.
      Оставить каждое округление SRAI15 отдельно, не объединять произведения.
- [x] Исследовать a0/a11/a12/a14 для четырёх immutable cache words.
      Сейчас a11=2 и a12=-4 нужны только маскам/выбору половины; a0 внутри
      post-rotation не используется. Проверить живость после цикла/ABI.
- [x] Forward t[i],t[N4+i] начинают с low-half, обратные
      t[N4-1-i],t[2*N4-1-i] с high-half. Проверить linked таблицы/границы
      и их противоположную фазу. Четыре слова читать только через итерацию;
      у обратных указателей на high-half сначала вычесть2 для aligned L32I.
- [x] Сохранить in-place порядок: оба задних PCM значения считываются
      ДО записи переднего результата; все четыре записи должны остаться
      в прежнем порядке. Проверить центральную пару, aliasing и все strides.
- [x] N4=480/240/120/60 даёт240/120/60/30 итераций, все чётные.
      Доказать это из реально linked mode; не ограничивать формат/битрейт.
- [x] Уместить переписанный цикл в276 B с недостижимым padding. Проверить
      exact PCM/RAM/ABI и10 A/B/A. Сокращение чтений само по себе не доказывает
      ускорение. Завершены30 A/B/A, оба high-bitrate gate проходят.

Собран [post-rotation pair](ESP8266_OPUS_ASM_MDCT_POST_PAIR.md):225 symbolic
пар,16 численных полных transform, точный in-place порядок,12 регрессий PASS.
Остальной ELF/RAM/frame прежние.30 A/B/A: CPU19287.66144 /87.48144 /87.67425%,
новый экспериментальный raw baseline.28 итоговых регрессий PASS/0skip,
RAM/frame прежние, все попытки/максимумы сохранены. Ordinary восстановлен
OTA/HTTP/WS; default OFF, цель70% и непрерывный I2S ещё не подтверждены.

Отдельно от изменения порядка/кэширования:

[MUL16S кандидат](ESP8266_OPUS_ASM_MDCT_MUL16.md) собран: полный CFG/range
анализ выделил16 из33 MULL, все low16 unsigned оставлены как есть. Меняются
только16 opcode-байтов, ABI/RAM/стек/расположение прежние. Проверки точности
проходят;30 физических A/B/A завершены. CPU19287.66965 /87.68683 /87.67142%,
выигрыша нет, оба speed gate FAIL: вариант отклонён.28 регрессий PASS,
PCM точен, RAM прежняя. Контрольный timeout/DRAM1044 B и все максимумы
сохранены; ordinary восстановлен OTA/HTTP/WS. Следом — post-rotation выше.

- [x] Проверить замену MULL на MUL16S в тех верхних произведениях MDCT,
      где **оба** операнда доказанно sign-extended int16 (`t` и `x>>16`).
      Это не относится к нижнему unsigned16 фрагменту `x & 65535`.
      Нужны проверка actual ISA/ширины, диапазонов, точный PCM и физический A/B;
      меньшая задержка MUL16S на LX106 пока не установлена. Не смешивать с
      предыдущим опытом четырёх MUL16U для вычисления high32 деления.

## 3. Порядок вычислений и регистры FFT butterflies

- [ ] Переставить независимые операции так, чтобы раньше записывать
      окончательный результат и освобождать регистр для следующего.
- [ ] Сократить повторные загрузки Fout, twiddle pointers и временные
      сохранения в стек; не увеличивать рабочие массивы или frame stack.
- [ ] Сопоставить зависимости MUL16/MULL, loads и stores в каждом butterfly,
      сохраняя исходный порядок округлений и wraparound арифметики.

Место: gcc/upstream/celt/kiss_fft.c.s, opus_fft_impl. Начинать с конкретного
горячего butterfly, не переписывать всю функцию одновременно и не разворачивать
все циклы. Меньшее число spills — гипотеза ускорения, не измеренные проценты.
Ранее отклонённый inner4 в renormalise_vector не является проверкой этой идеи.

При повторном чтении кода подтверждено: C_MUL_TABLE уже использует
yoradio_opus_table_load_pair для одного packed r/i слова (kiss_fft.c:47).
Не повторять это как новый опыт. Следующий независимый кандидат — исходный
пример4→3 загрузочных инструкции из пункта1 с сохранением8-byte диапазона
через выбор narrow/wide L32I, без исполняемого NOP и без новой RAM.
Сначала проверить фактические ширины, порядок mutable reads и отсутствие
переходов внутрь блока; это отличается от отклонённых load→MOV замен.

Подготовлен [FFT load3](ESP8266_OPUS_ASM_FFT_LOAD3.md): radix-5 .L54,
один8-byte диапазон, три L32I вместо четырёх (ширины3+2+3), без NOP.
Все регистры/упорядоченные data reads точны, RAM/stack/прочие адреса прежние.
Все30 A/B/A завершены: CPU19287.47015 /87.47965 /87.47535%, оба speed gate
FAIL, кандидат отклонён.33 итоговые регрессии PASS/0skip, PCM точен,
RAM/stack прежние; ordinary восстановлен OTA/HTTP/WS. Прежний load→MOV
вариант не используется как родитель. Следующая гипотеза ниже отличается
перестановкой независимого чтения, не просто уменьшением числа инструкций.

### Проверяемая следующая гипотеза: заполнить зависимость load-use

Статически в оригинальном FFT load3 первая загрузка указателя отделена от
его использования второй независимой загрузкой. В трёхинструкционном варианте
использование идёт сразу за L32I. Это зависимость данных, но не доказательство
числа stall cycles или причины результата на плате.

- [x] Отдельным кандидатом переставить соседний private `l32i a4,sp,100`
      между чтением указателя и PCM. Точный исходный диапазон лучшего
      post-pair ELF: 0x40254126..0x40254139,19 B. Не смешивать с PVQ.
- [x] Проверить последовательность: L32I a3,sp,32; L32I a4,sp,100;
      L32I.N a2,a3,0; L32I a3,a3,4; S32I a2,sp,156;
      S32I a3,sp,164; L32I.N a6,a4,0. Ширины3+3+2+3+3+3+2=19 B.
- [x] Сохранить порядок обоих PCM reads и stack stores. Перемещается только
      неизменяемое чтение sp+100, не alias к sp+156/164; фактический twiddle
      load a6 остаётся после записей. Проверить все live-регистры, SAR,
      границы блоков и события чтения/записи, не только выходной массив.
- [x] Выполнить отдельные10 A/B/A: отсутствие NOP/новой RAM и меньшее число
      инструкций не являются достаточным основанием принять оптимизацию.

Собран [FFT schedule](ESP8266_OPUS_ASM_FFT_SCHEDULE.md): семь инструкций
вместо восьми, тот же19-byte диапазон и остальные адреса. Символьная модель
не предполагает отсутствие alias между data pointers и записями в стек;
648 численных overlap/edge случаев. Все30 A/B/A завершены:
CPU19287.48044 /87.53837 /87.47348%, оба speed gate FAIL, кандидат отклонён.
39 итоговых регрессий PASS/0skip, PCM точен, RAM/frame прежние. Все попытки,
максимумы/accounting excess сохранены. Ordinary восстановлен OTA/HTTP/WS.
Default прежний; причина небольшого замедления не локализована.

## 4. Извлечение младших битов в ec_dec_bits

- [x] Проверить замену построения маски использованием уже вычисленной
      старшей части: high = word >> bits; low = word ^ (high << bits).
- [x] Сохранить весь entropy state: end_window, nend_bits, nbits_total,
      пополнение буфера, обработку исчерпания входа и возвращаемое значение.
- [x] Проверить допустимый диапазон bits, unsigned shifts, ABI и SAR;
      при необходимости восстановить исходное значение SAR.

Место: gcc/upstream/celt/entdec.c.s, ec_dec_bits, строка 1496.
Это отдельная арифметическая замена в общей функции, не повтор уже
отклонённого инлайнинга трёх вызовов ec_dec_bits(ec, 1).
Экономия инструкций зависит от требований к конечному SAR и регистрам;
ускорение полного декодера пока неизвестно.

### Уточнение linked ASM после partition-frozen, 2026-09-14

В принятом PVQ parent `ec_dec_bits` находится по0x40298548, размер80 B.
Общий хвост0x40298575..0x40298598 содержит14 инструкций: строит маску
через MOVI−1/SSL/SLL/XOR/AND, хотя одновременно вычисляет high=window>>bits.
Документированный контракт `entdec.h` допускает bits0..25 включительно.
Первый кандидат — low=window^(high<<bits), без изменения refill loop.
Предварительно это13 инструкций/33 B вместо14/35 B **с восстановлением SAR**;
экономия только одной инструкции, не обещание заметного CPU-выигрыша.

- [x] Собрать отдельный frozen-range хвост, padding только после RET;
      сохранить остальные байты ELF, entropy state и порядок записей.
- [x] Проверить unsigned bits0..25, все биты window, refill/exhaustion,
      ABI/callee-saved и SAR; caller-saved временные могут отличаться только
      в рамках доказанного call0 контракта, не на внутреннем общем переходе.
- [x] Независимые numeric/symbolic проверки готового ASM, host exact PCM
      до510 и10 A/B/A. Не принимать только по уменьшению14→13 инструкций.

[Опыт ec_dec_bits](ESP8266_OPUS_ASM_EC_BITS.md) завершён:32 symbolic,
81120 linked numeric,24 host PCM,30 физических A/B/A точны. Но CPU192
87.35529 /87.32860 /87.33156%, а12876.62877 /76.66525 /76.63775%:
оба speed gate FAIL. Кандидат отклонён, RAM/frame прежние. Все max/RAM
и341мкс window excess сохранены. Ordinary восстановлен OTA/HTTP/WS.

## 5. Повторное использование адресов строк PVQ

- [ ] В decode_pulses / cwrsi удерживать адрес текущей строки CELT_PVQ_U_ROW
      при поиске числа импульсов вместо повторного вычисления адреса.
- [ ] Проверить отдельно ветви many-pulses и many-dimensions, выходы поиска,
      нулевые координаты, знаки, оставшиеся две координаты и энергию yy.
- [ ] Не добавлять полный LUT или новые RAM-массивы и не пропускать
      энтропийные операции, даже при монофоническом выводе.

Место: gcc/upstream/celt/cwrs.c.s, decode_pulses, строка 265.
Это не прежний cache[lo] reuse в bits2pulses и не механическая замена
SLLI+ADD на ADDX4. Их отрицательные результаты остаются в силе.

### Уточнение после проверки linked decode_pulses, 2026-09-14

Обычный `for(p=row[k];...)` уже имеет pointer decrement: 0x40253338
уменьшает a6 на4, затем L32I читает row[k]. Повторного SLLI в этом back-edge
нет; не выдавать его устранение за новую оптимизацию.

- [ ] Исследовать именно many-dimensions развёрнутый поиск:
      0x402533b9..0x402534d7. Для k-2, k-3 и следующих проб снова выполняются
      SLLI + ADD к CELT_PVQ_U_ROW, хотя предыдущий адрес строки уже вычислялся.
- [ ] Попробовать сохранять адрес первой пробы ROW[k-1] в a0 и для следующих
      проб сдвигать указатель на4 вместо повторного SLLI+ADD. Не добавлять
      предварительную работу в ветвь нулевой координаты или первый успешный
      поиск. Вариант базового указателя с положительными L32I offsets проверять
      отдельно: его дополнительная инициализация может перевесить выигрыш.
- [ ] Доказать свободность a0 после единственного ec_dec_uint call0:
      исходный return сохранён в sp+44, восстановлен по0x40253584; frame48 B.
      Проверить все выходы поиска, отрицательный знак, _i/_k/_n/_y/yy и
      предыдущие значения временных регистров на общей точке продолжения.
- [ ] Не вводить новый предел k/n/битрейта по тестовым данным. Сверить
      адреса всех действительно выполняемых чтений с исходным ASM и таблицей;
      не читать лишнюю строку на границе и не пропускать entropy operations.
- [ ] Замерить отдельно при неизменной остальной компоновке и RAM,10 A/B/A.

Это пока статическая гипотеза: новое ASM и измерения для неё не выполнены.

[Host census PVQ](ESP8266_OPUS_PVQ_PATH_PROFILE.md) подтвердил редкость
дополнительных many-dimensions проб на192:1254/с против101079/с проб
many-pulses row-loop. Поэтому сначала проверить счётчик/обратный переход
этого уже pointer-based цикла: исключить MOV каждого прохода и вычисление
указателя, уже доступного через row[K]. Не утверждать, что исходный цикл
не имел pointer decrement. Новый кандидат требует отдельной проверки
всех выходных регистров, чтений, фиксированной компоновки и10 A/B/A.

Этот отдельный [PVQ row-loop](ESP8266_OPUS_ASM_PVQ_ROW_LOOP.md) выполнен:
35 B,6→4 команды в цикле, индукция/2298 numeric cases,30 A/B/A exact PCM.
CPU19287.46700 /87.30769 /87.49450%; оба gate PASS, RAM/frame прежние.
Принят как экспериментальный raw baseline, обычное радио восстановлено OTA.
Many-dimensions ROW[k-j] cache выше остаётся НЕ реализованным кандидатом.

## 6. Специализированные произведения со сдвигом в SILK

- [ ] Проверить места __muldi3 в silk_NLSF2A и LPC: можно ли получить
      именно требуемые биты произведения специализированным ASM.
- [ ] Для каждой замены доказать переносы, знаки, округление, wraparound
      и проверки выхода за int32; сохранить их для всех допустимых данных.
- [ ] Отделить частичные 32-битные произведения от неверного предположения,
      что исходное полное произведение всегда помещается в int32.

SILK LPC использует 64-битный результат в том числе для проверки
переполнения. Простое удаление старшего слова недопустимо.
Этот пункт полезен для SILK/hybrid, но имеет меньший приоритет для текущего
высокобитрейтного CELT корпуса. На LX106 нет MULSH/MULUH и MAC16;
готовую DSP-последовательность другого Xtensa нельзя переносить буквально.

## Порядок и критерии экспериментов

Начать с пункта 1, затем локального упрощения и парной обработки из пункта 2.
Остальные сохранять независимыми кандидатами с отдельными проверками точности.
По уточнению пользователя от 2026-09-14 микроправки с неразличимым отдельным
эффектом можно объединять для первого измерения пакета на плате. Не требовать
измеримого выигрыша от каждого компонента: применять критерии скорости ко
всей комбинации, проверив её совместимость и корректность. Подробные правила —
[«Накопление микроправок и проверка комбинаций» в основном плане](ESP8266_OPUS_ASM_BANDS_OPTIMIZATION_PLAN.md#накопление-микроправок-и-проверка-комбинаций).
Подтверждённые замедления учитывать отдельно; отсутствие заметного эффекта
не равно доказанному отсутствию эффекта. Старые результаты не переписывать.

- [ ] Сохранить recipe, комментарии функций/ABI, C fallback и default OFF.
- [ ] Сравнить ASM/linked CFG, нагрузки на стек, DRAM/IRAM и исходные данные.
- [ ] Изолировать влияние компоновки: по возможности отдельный frozen-layout
      контроль с прежними адресами остальных функций и литералов. Если
      нужны padding или дополнительные MOV, учитывать их как реальные инструкции.
- [ ] Проверить PCM и состояния на 12/24/64/128/192/320/510 кбит/с,
      различных длительностях пакетов, mono/stereo, PLC, reset и OOM.
- [ ] Минимум 10 физических запусков каждого варианта; предпочтительно
      A/B/A по 10. Сохранять все попытки, максимумы и минимумы свободной RAM.
- [ ] Принимать по критерию высоких битрейтов основного плана, а не по
      сокращению ROM или числу инструкций. Не приписывать разницу cache misses
      без отдельного доказательства.

Лучший raw результат теперь87.30769% CPU192 (tell-inline + MDCT half +
pre/post-rotation pairs + PVQ row-loop, экспериментально); текущая цель80% не достигнута. Ранее decode-only
quant_partition уменьшил app на 544 B, но дал около 98,32% вместо 88,13%
CPU192 и был [отклонён](ESP8266_OPUS_ASM_PARTITION_DECODE.md).
Это основание контролировать размещение, а не повторять компактные варианты
без новой проверяемой гипотезы.

Справочные основания:
[конфигурация LX106 Espressif](https://github.com/espressif/ESP8266_RTOS_SDK/blob/master/components/esp8266/include/xtensa/config/core-isa.h),
[выравнивание GNU assembler](https://sourceware.org/binutils/docs/as/Xtensa-Automatic-Alignment.html).
Таблицы гарантированных тактов для всех этих последовательностей нет;
статический обзор не заменяет измерение на ESP8266.
