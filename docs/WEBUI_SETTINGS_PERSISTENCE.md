# Сохранение настроек WebUI в native ESP-IDF

Этот документ — проверяемый чек-лист для общей страницы настроек YoRadio.
Основная текущая цель — ESP32-C3 OLED в client mode. В AP mode, как и в
Arduino-прошивке, показывается только настройка Wi-Fi.

Поле считается исправленным только тогда, когда выполняются все четыре
условия:

1. WebUI при открытии получает фактическое текущее значение.
2. Команда изменения проверяет диапазон и сохраняет значение.
3. Значение восстанавливается после полного отключения питания.
4. Настройка реально применяется, либо поле скрыто как неподдерживаемое.

Статусные значения (`ipaddr`, RSSI, bitrate, codec, buffer fill), кнопки
действий и поля отключённых аппаратных возможностей не относятся к
постоянным настройкам.

## Общие настройки воспроизведения

| Поле/команда | Хранилище | Применение | Статус |
| --- | --- | --- | --- |
| `volume` | NVS `audio/volume`, отложенная запись | коэффициент PCM | уже работает |
| `balance` | NVS `audio/balance` | баланс левого/правого каналов | уже работает |
| `current` / последняя станция | NVS `radio/last_station` | восстановление выбранной станции | уже работает |
| `smartstart` (`sst`) | NVS `radio/smartstart` | состояние запуска после питания | уже работает |
| `normalization` (`normalize`) | NVS `audio/normalize` | audio normalizer | уже работает |
| `normgain` | NVS `audio/normgain` | максимальное усиление | уже работает |
| `normtarget` | NVS `audio/normtarget` | целевой peak | уже работает |
| `normtime` | NVS `audio/normtime` | постоянная времени | уже работает |

## System

| Поле/команда | Требуемое поведение | Статус |
| --- | --- | --- |
| `audioinfo` (`aif`) | сохранять и включать/выключать параметры потока на OLED | [x] |
| `softap` (`softr`) | сохранять задержку перезагрузки AP 0..30 минут | [x] |
| `abuff` | сохранять размер encoded-буфера и применять при следующем старте | [x] |
| `mdnsname` (`mdns`) | DHCP hostname сохраняется; поле и `.local`-ссылка скрыты, пока mDNS component отсутствует | [x] |
| `watchdog` | сохранять и включать/выключать контроль зависшего TCP-потока | [x] |
| `telnet` | native telnet console отсутствует: скрывать поле, не имитировать сохранение | [x] |

## Screen / OLED

| Поле/команда | Требуемое поведение | Статус |
| --- | --- | --- |
| `screenon` (`dspon`) | сохранять и управлять питанием SSD1306 | [x] |
| `numplaylist` (`nump`) | сохранять и добавлять номер к названию станции | [x] |
| `stationuppercase` (`upst`) | сохранять и применять регистр названия | уже работает |
| `brightness` (`br`) | сохранять и применять яркость 0..100% | уже работает |
| `screensaverenabled` (`scre`) | сохранять screensaver остановленного радио | [x] |
| `screensavertimeout` (`scrt`) | сохранять таймаут 5..65520 секунд | [x] |
| `screensaverblank` (`scrb`) | сохранять режим blank/power-off | [x] |
| `screensaverplayingenabled` (`scrpe`) | сохранять screensaver при воспроизведении | [x] |
| `screensaverplayingtimeout` (`scrpt`) | сохранять таймаут 1..1080 минут | [x] |
| `screensaverplayingblank` (`scrpb`) | сохранять режим blank/power-off при playback | [x] |

## Timezone / SNTP

| Поле/команда | Требуемое поведение | Статус |
| --- | --- | --- |
| `tzh` | сохранять смещение часов -12..14 | [x] |
| `tzm` | сохранять минуты 0/15/30/45 | [x] |
| `sntp1` | сохранять и применять основной SNTP server | [x] |
| `sntp2` | сохранять и применять резервный SNTP server | [x] |
| `timeint` | сохранять и применять интервал SNTP 15..1440 минут | [x] |

## Controls

| Поле/команда | Требуемое поведение | Статус |
| --- | --- | --- |
| `volsteps` (`vols`) | сохранять шаг громкости 1..10 и применять к `volp`/`volm` | [x] |
| `oneclickswitching` (`skipup`) | на C3 нет экранного списка/энкодера: скрывать как неприменимое | [x] |

## Wi-Fi

| Поле | Требуемое поведение | Статус |
| --- | --- | --- |
| `ssid0..4`, `pass0..4` | атомарно сохранять все непустые строки `wifi.csv`, читать их обратно и пробовать сети по порядку | [x] |

## Не показываются на ESP32-C3 OLED

Эти поля принадлежат другим аппаратным профилям и не должны сохраняться в
сборке C3: VU meter, TFT flip/invert, contrast, touch, encoder acceleration,
IR tolerance, RTC interval, weather и equalizer.

## Проверка

- [x] Host-тест проверяет отсутствие фиктивных констант в ответах `getsystem`,
  `getscreen`, `gettimezone`, `getcontrols`.
- [x] Host-тест проверяет обработчик каждой команды из таблиц.
- [x] Полный `npm test` проходит (171 тест).
- [x] ESP32-C3 native собирается с `-O3`.
- [x] На плате значения меняются через WebUI, переживают reset и возвращаются
  актуальными при повторном открытии страницы.
