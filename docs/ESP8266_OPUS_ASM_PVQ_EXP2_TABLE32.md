# Opus ASM: прямое чтение exp2_table8 как 32-битных слов

2026-09-15. Независимый эксперимент поверх принятого `pvq-index-half`
(80.019979% raw CPU192), не поверх ещё не измеренных logN/exp2 helpers.
2026-09-17: аппаратные30 A/B/A завершены;79.626521% raw CPU192.
Принят экспериментально, не выбран по умолчанию. Raw<=80% выполнено,
78% и полная цель с непрерывным I2S/WebUI ещё не подтверждены.

## Изменение

В quant_partition меняются только два трёхбайтных оператора:

- `0x4024dc6b`: SLLI a4,a3,1 → SLLI a4,a3,2;
- `0x4024dc76`: L16SI a3,a3,0 → L32I a3,a3,0.

Literal по0x40211a9c теперь указывает на0x4024dd28. Там размещена копия
восьми исходных signed16 значений, каждое со знаковым расширением до32бит.
Исходная16-байтная таблица по0x402d74d8 остаётся неизменной для других копий
compute_qn. Новая32-байтная таблица занимает доказанно недостижимый для
декодера encoder-код. Новых секций, RAM, стека или увеличения образа нет.

Не добавлены переходы, вызовы или phase-helper. В проверяемом участке
семь видимых инструкций как до, так и после изменения. Работа SDK-обработчика
узкого flash-чтения в это число не входит; это не измерение числа тактов.
По сравнению с предыдущим exp2 helper устраняются его дополнительные операции.
Разницу времени и влияние flash-кэша всё равно нужно проверить на плате.

a4 временно содержит4*i вместо2*i, но прежний MOVI a4,14 немедленно
перезаписывает его. В точке0x4024dc7b все GPR и SAR совпадают с контролем.
Символически проверены все32 входных бита qb и все16 бит значения таблицы;
перебираются только восемь возможных индексов. Frame quant_partition112байт
и все инструкции вне двух заменённых операторов остаются прежними.

## Указатель и безопасное размещение

Обычный линейный objdump недостаточен: после недостижимых байтов он может
ошибиться с границей инструкции. Поэтому проверяется каждое байтовое положение
кодировки L32R во всех allocated executable секциях ELF. Найдены два шаблона:

- `0x4024dc6e` — единственный настоящий читатель literal;
- `0x4021d1d3` — середина L32I a12,a1,200 в неизменённой time_service_poll,
  а не начало инструкции. Это отдельно проверено по достижимому CFG и байтам.

Allocated указателей на адрес самого literal нет. Новая ссылка или новое
совпадение отклоняются. Проверены границы table-slot0x4024dd28..0x4024dd48,
все11 прежних инструкций, предшествующий ADD и входящие ветви: они недостижимы
при доказанном private encode=0. В текущем декодере в этот слот нет входов.
Он не пересекается ни с logN helper, ни с прежним exp2 helper.

Это **альтернатива** exp2 word-helper, а не дополнительный патч к нему:
оба изменяют одно чтение. После отдельных A/B выбрать один вариант.
Совместимость с logN проверить отдельной сборкой и отдельными измерениями;
проценты выигрыша не складывать.

## Проверки и артефакты

- [x] Сборка реальным LX106 assembler/linker, неизменные размеры секций/функций.
- [x] 131072 signed-теста и 3904 теста реальных qb/SAR; всего 134976 случаев.
  До/после944832 видимых инструкции в этом корпусе, все регистры совпадают.
- [x] 24 host-сценария с ASan/UBSan, exact PCM/state/PLC/reset/OOM,
  320/510кбит/с, phase/mixed и120мс/48-frame packets; максимальная ошибка PCM0.
- [x] 6 focused negative/positive tests PASS/0skip, 91.73 с; журнал сохранён.
- [x] Общий связанный прогон: 184 PASS, 0 ошибок/пропусков, 335.34 с.
  Полный final-tests.log сохранён рядом с бинарником.
- [x] 10 A/10 B/10 A2 на плате: CPU-медианы, максимумы, все неудачи, RAM/стек.
- [ ] Затем >=20с непрерывного I2S PDM и работающий WebUI.

CPU160МГц/runtimeQIO40. Образ903216байт, четыре диапазона патчей42байта.
Кандидат SHA256:
`7209b07ddde4febf7078e90100bfac3b3e4d9a7538e1ce44beaac95c30738649`.
Контроль SHA256:
`0d76ec891f194e58e385339f1777e38c187b1bd0616a3c3bc8dfce91a033468e`.

Файлы: `firmware/development/esp8266-opus-pvq-exp2-table32-{control,candidate}-v1`.
Рецепт/ASM/proof: `tools/esp8266_opus_asm/pvq_exp2_table32*`.
C fallback сохранён; host-модель меняет только non-stereo compute_qn,
соответствующий этому quant_partition, не stereo theta.

```powershell
node tools/esp8266_opus_asm/check_bands.cjs pvq-exp2-table32
node tools/esp8266_opus_asm/pvq_exp2_table32.cjs
node --test tests/esp8266-opus-pvq-exp2-table32.test.js
```

Аппаратные серии: `.build/opus-pvq-exp2-table32-board/{before,candidate,after}`.
OTA-отчёты ota-before/candidate/after.json; run_raw_series.ps1 Attempts10,
IntervalMs15000. report_pvq_exp2_table32.cjs требует все30 попыток и отдельно
вычисляет прежний80%-критерий и запрошенный78%-ориентир.

Первый свежий запрос /api/native/status снова завершился таймаутом.
Ни OTA, ни UART/сброс, ни изменение сетевых настроек компьютера не выполнялись.
Для этих бинарников аппаратная скорость и runtime free RAM пока не измерены.
Проверка старых результатов в регрессиях не является новым замером платы.

Повторный и финальный запросы после проверок также завершились таймаутом.
Все три наблюдения сохранены в board-initial/recheck/final-unavailable.json.
Локальный маршрут192.168.100.0/24 присутствует, но ARP не разрешает MAC платы.
Это не доказывает причину недоступности: питание, Wi-Fi и зависание нельзя
различить по этим данным. Прошивка устройства в этом эксперименте не менялась.

Недоступность повторилась в трёх последовательных продолжениях цели. В них
выполнена полезная локальная работа: logN, exp2 helper и данный table32
подготовлены с доказательствами и тестами. Теперь обязательный следующий
этап — физические A/B/A, выбор одного exp2 варианта, затем комбинация с logN
и >=20с I2S/WebUI. Он заблокирован до возвращения платы в локальную сеть;
host-тайминги не заменяют измерения LX106. Подтверждённый CPU192 остаётся
80.019979%, требования <=80% и следующий ориентир78% ещё не выполнены.

## Физические результаты2026-09-17: принято экспериментально

История недоступности выше относится к15сентября. После возвращения платы
OTA и все30 raw-попыток успешны; точные PCM-хэши совпали на12/24/64/128/192.
Первый контроль — те же10 измерений, что A2 logN-эксперимента; это явно
общая серия, не дополнительные10 запусков. Прямой table32 не содержит logN helper.

| кбит/с | A CPU% | B CPU% | A2 CPU% |
| ---: | ---: | ---: | ---: |
| 12 | 23.092687 | 23.083750 | 23.098063 |
| 24 | 54.116229 | 54.073604 | 54.128083 |
| 64 | 62.659833 | 62.660979 | 62.623646 |
| 128 | 72.029958 | 71.868875 | 72.011875 |
| 192 | 80.047250 | 79.626521 | 80.044312 |

Относительное ускорение192:0.526/0.522%;128:0.224/0.199%.
Небольшое замедление64:0.0018/0.0596%, меньше выигрыша на обоих высоких
битрейтах. Оба selection-gate проходят. Raw80% выполнено без округления;
до78% остаётся1.626521п.п., или2.042687% времени декодера (325.304мкс/20мс).

Static RAM/IRAM/stack delta0, app903216B. Минимум DRAM A/B/A2:
8184/8176/3628B; на192:9808/9800/9800B. Временные изменения сетевой кучи
не означают изменение размера декодера. Stack watermark1660B во всех группах.
Maximum192:22287/21482/20402мкс — улучшения максимума против обоих контролей
нет, непрерывность I2S из средней загрузки не следует.

HTTP observation errors0. Task>wall сохранены без вычитания: A mono12
run2/run7 +1816/+1270мкс; B mono12 run1/run10 +559/+726мкс. Разные окна
измерения не позволяют считать эти разности временем чистого декодирования.

Все30 JSON/logs, OTA и пересчитанное comparison.json сохранены рядом с app.bin.
Связанный локальный прогон18 tests PASS/0skip,118.99с: ASM proofs,
host PCM metadata, CPU gates, целостность аппаратных доказательств.
Отдельная регрессия пересчитывает новые30 замеров и общий контроль.
Следующий шаг — измерить альтернативный exp2 helper, затем лучший вариант
на полном I2S/HTTP тракте. Не складывать выигрыши взаимозаменяемых вариантов.

Продолжение17сентября: exp2 helper отклонён, table32 остался лучшим.
Полный I2S/HTTP тест дал0/10 квалифицированных окон; затем прежняя
C radio-сборка восстановлена OTA. [Live-отчёт и следующие шаги](ESP8266_OPUS_WORD_LOAD_BOARD_20260917.md).
