# TODO: перенос возможностей ESP32-C3 OLED native в ESP32 CYD native

Цель: привести ESP32 CYD native к возможностям ESP32-C3 OLED native и
оригинальной Arduino-прошивки YoRadio, сохранив аппаратные преимущества CYD:
двухъядерный ESP32, TFT, touch, SD и варианты PDM/DAC.

Основной принцип: переносить общие функции в единый native ESP-IDF-компонент,
а не копировать реализации между каталогами плат. В каталогах плат должны
оставаться только аппаратные адаптеры, GPIO и профили возможностей.

## Приоритет 0: WebUI и управление радио

- [ ] Вынести HTTP-сервер и WebSocket-протокол C3 в общий native-компонент.
- [ ] Подключить CYD к общему `/ws` и полному начальному snapshot WebUI.
- [ ] Передавать Play, Connecting, Playing, Stop, RSSI, codec, bitrate,
  sample rate, channels, buffer fill и ICY metadata.
- [ ] Сохранить асинхронную отправку статусов: аудио и HTTP handlers не должны
  блокироваться на рассылке WebSocket.
- [ ] Поддержать reconnect, heartbeat и восстановление полного состояния.
- [ ] Обновлять список станций только после реального успешного изменения
  плейлиста.
- [ ] Не перестраивать и не прокручивать список при обычных статусных событиях.
- [ ] Оставить `/api/native/*` временно для совместимости и затем объявить его
  устаревшим.

## Приоритет 0: модель радио и плейлист

- [ ] Перенести `radio_control` в общий native-компонент.
- [ ] Загружать и индексировать `/data/playlist.csv`.
- [ ] Создавать индекс при запуске только при его отсутствии или устаревании.
- [ ] Перестраивать индекс после успешной загрузки нового плейлиста.
- [ ] Реализовать выбор станции, Play/Pause/Stop, Next и Previous.
- [ ] Сохранять последнюю выбранную станцию в NVS и восстанавливать её.
- [ ] Выделять текущую станцию в WebUI без повторной загрузки списка.
- [ ] Добавить источник SD и переключение Web/SD для CYD.

## Приоритет 0: аудиопоток и переключение декодеров

- [ ] Перенести координацию поколений потока и освобождения декодера.
- [ ] До открытия нового HTTP/TLS-соединения освобождать несовместимый
  декодер и его arena/workspace.
- [ ] При одинаковом семействе декодеров повторно использовать подходящий
  workspace без лишних `malloc/free`.
- [ ] Проверить переходы MP3, AAC, FLAC, Vorbis и Opus во всех порядках.
- [ ] Определять кодек по содержимому потока; URL использовать только как
  неавторитетную подсказку.
- [ ] Добавить custom FLAC как альтернативу через Kconfig и сравнить его с
  декодером Espressif на CYD.
- [ ] Сохранять Espressif MP3 декодером по умолчанию; Helix и minimp3 оставить
  только сборочными вариантами.
- [ ] После Stop немедленно прекращать TCP/HTTPS-чтение, останавливать декодер
  и очищать старые сведения о потоке.
- [ ] Добавить явные сообщения об ошибках выделения памяти и открытия
  декодера в debug-консоль и пользовательский статус.

## Приоритет 1: ICY и параметры потока

- [ ] Использовать общий стандартный ICY parser.
- [ ] Публиковать `StreamTitle` без фильтрации корректно полученного текста.
- [ ] Очищать ICY metadata при Stop и смене станции.
- [ ] Сначала использовать реальный битрейт декодера, затем `icy-br` как
  резервный источник.
- [ ] Обновлять битрейт при его реальном изменении.
- [ ] Сохранять стабильный снимок sample rate/channel count только для
  прокручиваемой строки дисплея; реальные изменения формата должны обновлять
  PCM-тракт и WebUI (уточнение от 2026-09-10, см. список ниже).
- [ ] Публиковать codec, bitrate, sample rate и channels в WebUI и на TFT.

### Исправления формата потока — аудит 2026-09-10

Пункты ниже ещё не реализованы. Точный дефект ESP8266 «всегда mono» не найден.
Штатный Espressif-путь получает `info` и передаёт его в `send_pcm()` каждый
кадр; сохраняем это поведение. Ошибка первого формата касается альтернативного
`custom_legacy_output()` (Helix и другие legacy backends).

- [ ] В `custom_legacy_output()` обновлять `context->stream_info` при реальной
  смене частоты/каналов/разрядности, а не только при `!stream_info_ready`.
  Передавать в `send_pcm()` формат текущего блока; обновлять пользовательский
  статус, даже если bitrate при смене mono/stereo остался прежним.
- [ ] Перенести в общий Helix AAC adapter разделение исходной частоты
  `AACGetStreamSampRate()` и фактической частоты PCM `AACGetSampRate()`.
  Статус HE-AAC должен учитывать SBR в потоке даже при отключённом SBR-синтезе;
  PCM/I2S должны продолжать использовать реальную частоту декодированных данных.
- [ ] Согласовать с C3 разделение source channels / output channels при mono
  downmix; не делать вывод о PS stereo без подтверждения от декодера.
- [ ] Обновлять WebUI/snapshot при изменении формата, не создавать рассылку
  одинаковых статусов на каждый кадр. Проверить очистку при Stop/смене станции.
- [ ] Добавить исполняемые регрессии: stereo -> mono -> stereo, смена частоты
  в одном потоке с неизменным bitrate, AAC-LC/HE-AAC, отличающиеся stream/PCM
  rate, неизменность PCM и длительности, Stop/Play и восстановление статуса.
- [ ] Проверить сборки с AAC Espressif (штатный профиль) и AAC Helix
  (альтернативный), затем WebUI/TFT и воспроизведение на физической CYD.

Код: [audio_service.c](../idf/esp32-cyd2usb-native/main/audio_service.c),
[общий Helix adapter](../idf/components/custom_legacy_codecs/custom_legacy_adapter.cpp).
Эталон ESP8266: `0645c41`, `8a6d2ce`.
Общий adapter исправлять один раз с проверкой обеих плат:
[TODO ESP32-C3 OLED](ESP32C3_OLED_NATIVE_TODO.md).

## Приоритет 1: нормализация и DSP

- [ ] Подключить к CYD общий fixed-point нормализатор и soft limiter.
- [ ] Обрабатывать PCM блоками, использовать кэш параметров и заранее
  рассчитанные коэффициенты сглаживания.
- [ ] Сохранять настройки нормализации в NVS и возвращать актуальные значения
  при открытии WebUI.
- [ ] Оставить время нормализации по умолчанию 5 секунд.
- [ ] При смене станции сбрасывать накопленное дополнительное усиление в 0 dB.
- [ ] Проверить нагрузку нормализатора отдельно для PDM, continuous DAC и
  legacy DAC.

## Приоритет 1: настройки и сеть

- [ ] Перенести общую модель runtime settings и capabilities.
- [ ] Сохранять аудиобуфер, watchdog, mDNS, timezone/SNTP, шаг громкости и
  ускорение энкодера.
- [ ] Сохранять все Wi-Fi-профили из `wifi.csv`, включая порядок выбора.
- [ ] Предупреждать о пробелах в начале или конце SSID без автоматического
  изменения введённого имени.
- [ ] В режиме AP показывать только Wi-Fi-настройки; в режиме client — полную
  страницу настроек.
- [ ] Публиковать актуальный RSSI периодически, не блокируя аудиотракт.
- [ ] Не обновлять поля формы настроек непрерывными статусными сообщениями.

## Приоритет 2: аппаратное управление

- [ ] Вынести EC11/KY-040 encoder input в общий необязательный компонент.
- [ ] Добавить CYD Kconfig-профиль GPIO энкодера с проверкой конфликтов с TFT,
  touch, SD и аудиовыходом.
- [ ] Направлять WebUI, touch, encoder, IR и другие источники управления в
  единые функции `radio_control_*()`.
- [ ] Публиковать в WebUI изменения Play/Stop/Next/Previous, вызванные
  физическими органами управления.
- [ ] Реализовать общий кратковременный статус `playing`, `stopped`, `next`
  или `prev`; CYD должен показывать его в своей TFT-области статуса, а не
  копировать OLED-разметку второй строки.

## Приоритет 2: CYD-интерфейс и production profile

- [ ] Заменить временную цветовую заливку CYD полноценным YoRadio TFT UI.
- [ ] Добавить станцию, песню, параметры потока, RSSI, буфер и состояние
  воспроизведения.
- [ ] Реализовать CYD-specific touch, SD и display capabilities.
- [ ] Создать CYD production profile с теми же исходниками, что debug-сборка;
  различия должны задаваться только макросами логирования и sdkconfig.
- [ ] Сохранить вывод явных критических ошибок даже в production-профиле.

## Уже перенесено — повторно не делать

- [x] Входной compressed ring buffer 16 KiB.
- [x] PCM ring buffer 8 KiB.
- [x] TLS record buffers 16 KiB RX / 4 KiB TX.
- [x] Espressif MP3 выбран по умолчанию.
- [x] Compile-time варианты MP3/AAC декодеров.
- [x] Q15 volume и balance.
- [x] Сохранение volume и balance в NVS.
- [x] PDM resampling для фиксированной частоты аппаратного вывода.
- [x] Запись в ring buffer через `xRingbufferSendAcquire()` без постоянного
  выделения элементов в общей куче.

## Не переносить буквально

- [x] OLED-шрифты, логотип 72x40 и прокрутку SSD1306.
- [x] OLED brightness mapping и разметку двух строк.
- [x] C3 BOOT GPIO9 ISR и C3 GPIO assignments.
- [x] Одноядерное распределение задач ESP32-C3.
- [x] Стерео sigma-delta/PDM GPIO C3.

Семантику управления и статусов из этих функций нужно сохранить через общий
слой, но отображение, GPIO и планирование должны оставаться адаптерами CYD.

## Проверка flash и RAM

- [ ] После каждого крупного этапа проверять размер application image.
- [ ] Сохранить не менее 10% запаса в каждом OTA app-разделе.
- [ ] Измерить free heap и largest free block после загрузки, Wi-Fi, TLS
  handshake, открытия декодера и первого PCM frame.
- [ ] Проверить HTTPS + WebUI + TFT одновременно.
- [ ] Проверить длительное воспроизведение и многократное переключение
  смешанных кодеков без утечек и фрагментации.

Базовая CYD-сборка от 20 августа 2026 года занимает 1,320,320 байт из
1,769,472 байт app-раздела: свободно около 449 KiB (25%). Запас flash достаточен
для начала унификации, но RAM должна проверяться на реальной плате после
каждого этапа.

## Общая матрица приёмки

- [ ] WebUI открывается без зависшего spinner.
- [ ] После reconnect список станций не исчезает.
- [ ] Play проходит через Connecting в Playing и блокирует повторную команду
  на время подключения.
- [ ] Stop, Next, Previous и выбор станции работают из WebUI и с устройства.
- [ ] Физические команды немедленно отражаются в WebUI.
- [ ] Текущая станция выделяется без ненужного обновления списка.
- [ ] При загрузке страницы список один раз прокручивается к текущей станции.
- [ ] Playlist changed отправляется только после успешной замены файла.
- [ ] ICY title, codec, bitrate, sample rate, channels и RSSI обновляются.
- [ ] AP показывает только Wi-Fi settings, client — полные settings.
- [ ] MP3/AAC/FLAC/Vorbis/Opus и HTTP/HTTPS проверены на низком и максимальном
  битрейте.
- [ ] Переключение кодеков не вызывает пауз, утечек, deadlock или decoder/TLS
  overlap.
- [ ] Настройки переживают reset и отключение питания.
- [ ] Один набор host protocol tests выполняется для C3 и CYD.
- [ ] Добавлены browser tests и hardware-in-loop сценарии через COM-порт.

Связанный стратегический документ:
[NATIVE_FIRMWARE_PARITY_PLAN.md](NATIVE_FIRMWARE_PARITY_PLAN.md).
