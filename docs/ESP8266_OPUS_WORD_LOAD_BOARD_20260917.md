# ESP8266 Opus ASM: физическая проверка word-load,17сентября2026

Итог: **79.626521% CPU** для чистого декодера192кбит/с, но полный
тракт Opus/I2S ещё НЕ квалифицирован.78% не достигнуты. Продакшн-профиль
не изменён. Все OTA — только app; без UART/сброса, изменения Wi-Fi компьютера,
перезаписи SPIFFS/NVS или пользовательского плейлиста.

## Три независимых эксперимента

CPU160МГц, runtimeQIO40, одинаковые заранее загруженные RAM-пакеты,
без аудиовывода и function/stage profiling внутри raw-теста. CPU-время задачи
исключает другие задачи, но включает приписанные ей ISR/измерительную обвязку.

| Изменение поверх index-half | CPU192, медиана | Решение |
| --- | ---: | --- |
| logN phase-helper | 80.005042% | Отклонён: медленнее128/64 |
| Прямая exp2 table32 | 79.626521% | Принят экспериментально |
| exp2 phase-helper | 80.328125% | Отклонён: медленнее128/192 |

Каждый кандидат проверен10 A/10 B/10 A2. Последовательность:
A1/logN/A2/table32/A3/exp2-helper/A4. Всего70 уникальных физических запусков;
общие A2/A3 не считать дважды. Все PCM-хэши совпали. Все неудачи наблюдения,
максимумы и task>wall случаи сохранены без вычитания или фильтрации.

У table32 выигрыш192 составляет0.526/0.522% относительно двух контролей,
128 —0.224/0.199%; статическая RAM/IRAM/стек и app903216B не выросли.
Наблюдавшийся минимум192 heap9800B, watermark стека1660B. Эти raw-значения
не означают такой же запас при живом HTTP-потоке. Максимальный вызов192
21.482мс не доказывает непрерывность DMA. До78% нужны ещё1.626521п.п.,
или2.042687% времени декодера /325.304мкс на20мс аудио.

Подробные таблицы и воспроизведение:
[logN](ESP8266_OPUS_ASM_PVQ_LOGN_WORD.md),
[table32](ESP8266_OPUS_ASM_PVQ_EXP2_TABLE32.md),
[exp2 helper](ESP8266_OPUS_ASM_PVQ_EXP2_WORD.md).
Связанный локальный набор18 tests PASS/0skip,118.99с. Новый тест архивов
проверяет все70 уникальных запусков, хэши, PCM, OTA-слоты и пересчёт критериев.

## Полный путь:0 из10 успешных окон

Тот же table32 app установлен заново. Собственный60-секундный тонально-шумовой
файл: Opus CBR192кбит/с, stereo48кГц,20мс/packet; Ogg page20мс.
HTTP-источник192.168.100.253:8765/test.opus, только локальная сеть.
Файл1527630B, длительность с preskip60.0065с;203661бит/с вместе с Ogg,
а не192кбит/с полезного кодированного звука. SHA256:
`2f0c425a525f6d21bdc4c4ed0c070d9abe96fa71848102c1407538cad50ba028`.

Каждый запуск:5с на старт, затем два health/profile снимка с интервалом27с;
запрошенное квалифицирующее окно25с. Все10 POST приняты, но ни одно окно
не доказало непрерывность. Начальный RSSI-55, полученные startup-статусы-57..-60dBm.

- Первая попытка:171528 PCM frames всего (3.5735с аудио, НЕ доказанно
  непрерывных);90512 принятых байт. Startup status и первый health запрос
  завершились таймаутом. В последнем снимке transport_result4/errno116,
  PCM не продвигался около39с. Сервер сохранил первоначальный GET и повторный
  GET через17.5с; это согласуется с автоматическим переподключением.
- После этой попытки diagnostic stage8: запрос scratch6144B, общий свободный
  DRAM10032B, largest5076B. Это снимок последующего отказа инициализации,
  не доказательство, что первая попытка вовсе не декодировала звук.
- Попытки2..10: получен DECODER INIT ERROR. Stage8, free DRAM8372..8552B,
  largest5076B при scratch6144B; защитный резерв4096B не снижен.
  После освобождения current DRAM23540..23676B, largest17676..17788B.
  Это указывает на недостаток подходящего блока во время старта; не доказывает
  утечку и не объясняет само по себе первоначальный сетевой таймаут.
- Boot-lifetime min_heap4676B, audio stack watermark1468B. Это не минимум
  непрерывного25-секундного воспроизведения: такого интервала не получено.
  При остановленном радио DMA счётчики продолжают нейтральный вывод;
  их прирост в отказавших окнах нельзя выдавать за частоту звуковых провалов.

В raw stereo корпусе scratch реально достигает5488B. Простое уменьшение до5КиБ
не подходит. Полный живой путь включает HTTP, Ogg, нормализацию и PDM;
успешный raw80%-gate не заменяет эту отдельную проверку.

## WebUI и восстановление

На table32: root HTTP200,104.090мс, gzip27249B; WebSocket getindex и
плейлист доступны после отказа. Визуальная проверка НЕ выполнена:
две попытки запуска встроенного браузера после reset завершились
`windows sandbox failed: helper_unknown_error: setup refresh had errors`.
Это сбой инструмента; HTTP-проверка не объявляется браузерной или нагрузочной.

Восстановлен заранее сохранённый C-профиль
`firmware/development/esp8266-opus-live512-idle3s-20260913/app.bin`,
SHA256 `661becd301b07885d493ceb1b513d9e874b7d86e4ada8231da8657aa90983c4b`.
Это прежняя диагностическая radio-сборка, benchmark OFF, не новая
квалифицированная production-сборка. OTA app0x10000→0x110000 проверен.
Радио остановлено, ошибок нет, станция167, volume100/balance0; плейлист
побайтно совпадает по SHA256. RSSI-57, free heap27628B, root200 за97.105мс;
WebSocket передаёт исходное состояние. Локальный fixture-сервер остановлен.

Все10 live-run/trace/log, серверный журнал, бинарный fixture, исходный/конечный
снимки и OTA хранятся в
`firmware/development/esp8266-opus-pvq-exp2-table32-candidate-v1/live-20260917/`.
Прежние недоступные наблюдения15сентября не удалены и не подменены.

## Следующие шаги (не реализованы этим опытом)

- [ ] Проследить возврат close/socket ownership и причину потери сохранённого
  Opus workspace при reconnect. Код уже пытается сохранять его при успешном
  close; нельзя добавлять вторую такую же логику или сохранять повреждённый
  декодер вслепую. Сопоставить с TCP RX/ожиданием, без UART-команд.
- [ ] Измерить largest/free вокруг каждого codec/input/PCM/workspace/state/
  scratch выделения. Проверить порядок «крупные блоки первыми» и объединение
  совместимых времён жизни, не увеличивая RAM и сохраняя cleanup на каждом OOM.
  Не снижать6144 scratch или4096 reserve ради прохождения теста.
- [ ] Объяснить исходный HTTP idle timeout отдельно от последующего OOM;
  сохранить все попытки и метрики. Не считать сильный RSSI доказательством
  отсутствия потерь, а данные, записанные сервером в TCP, — принятыми платой.
- [ ] После устойчивого старта повторить>=10 живых проверок,>=20с без новых
  active DMA underruns, затем [cooperative PCM drain](ESP8266_OPUS_COOPERATIVE_OUTPUT_TODO.md).
- [ ] Для78% продолжить точную qn lookup/logN на полосу/byte-phase план;
  новый ASM baseline — table32. Полная цель остаётся незавершённой.

## Воспроизведение собственного файла

FFmpeg8.1.1, без чужих записей. Не перезаписывать архивный fixture:

```powershell
ffmpeg -hide_banner -loglevel error -n -f lavfi -i 'aevalsrc=0.24*sin(2*PI*997*t)+0.04*sin(2*PI*10007*t)|0.19*sin(2*PI*1703*t):s=48000:d=60' -f lavfi -i 'anoisesrc=color=white:amplitude=0.025:seed=7349:r=48000:d=60' -filter_complex '[0:a][1:a]amix=inputs=2:normalize=0' -ac 2 -c:a libopus -application audio -b:a 192k -vbr off -frame_duration 20 -page_duration 20000 -fflags +bitexact -flags:a +bitexact tone-noise-192.opus
node tools/esp8266_opus_profile/serve_fixture.cjs --file tone-noise-192.opus --host 192.168.100.253 --port 8765 --output server.jsonl
node tools/esp8266_opus_profile/run_live_series.cjs --url http://192.168.100.253:8765/test.opus --directory .build/new-live-series --attempts 10
```
