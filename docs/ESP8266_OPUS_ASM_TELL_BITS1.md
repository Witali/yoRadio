# Opus ASM: tell-inline + чтение одного entropy-бита

2026-09-13. Отдельный эксперимент `bands-tell-bits1-asm`, default не изменён.
Контроль — сохранённый лучший `bands-tell-inline-asm`, не исходный GCC-ASM.
Результат: кандидат отклонён, все пять потоков замедлились. Цель 70% не достигнута.

## Изменение и точность

Три вызова `ec_dec_bits(ec, 1)` в `quant_band`/`quant_all_bands` заменены
специализированным GAS-макросом. Если в end_window уже есть бит, нужны только
младший бит, постоянный сдвиг и обновление трёх полей. Исчезают вычисление
маски и переменные сдвиги через SAR. Если окно пустое, вызывается прежняя
GCC-функция с неизменными аргументами/состоянием. Пополнение пакета, нулевые
биты при его исчерпании и остальные вызовы не меняются.

ABI, поля, регистры и ограничения описаны в `bits1_fast.inc.s`.
Контекст читается/пишется только выровненными 32-битными инструкциями.
В первом месте a3 получает N из a12 после проверки N==1; в двух других
a3 явно равен 1. Encoder-пути не изменены. Stack/arena/таблицы не добавлены.
Четыре исходных tell-inline места и C fallback сохранены.

- [x] Reproducible overlay, проверка protected C/GCC hashes и прочих object-секций.
- [x] 100 000 интерпретаций реального макроса: результат, состояние, ABI, cold path.
- [x] Host ASAN/UBSAN: точный PCM 12/24/64/128/192, mixed/PLC/reset/OOM/guards.
- [x] Linked ELF: статическая RAM, стек, адреса, фактические инструкции.
- [x] 10 свежих контролей + 10 кандидатов + 10 повторных контролей; все попытки.
- [x] Решение по результатам и возвращение обычной прошивки через OTA.

Host-модель проверяет математику, а не скорость LX106. Ускорение не заявлено
до физических замеров. Рост/смещение flash-кода может изменить кэш и linker
relaxation; выигрыш нельзя выводить только из количества команд.

## Воспроизведение

```powershell
node tools/esp8266_opus_asm/tell_bits1.cjs <xtensa-lx106-elf-gcc.exe>
node --test tests/esp8266-opus-tell-bits1-asm.test.js tests/esp8266-opus-tell-inline-asm.test.js
node tools/esp8266_opus_asm/check_bands.cjs tell-bits1
```

Сборка через `build_i2s_pdm_production.ps1`, Diagnostic/EnableOpus,
OpusBackend=bands-tell-bits1-asm, OpusWordAsm/OpusIcdfFlashWord/OpusFirFlashWord,
NoSpiffsCache/Pdm32Iram/Pdm32Batch/OpusBenchmark и прежние fixtures.
Только OTA; GPIO3/UART, разметка и SPIFFS не изменяются.

## Физические результаты

Исходник сборки: `5a96989a`. CPU160/QIO40, 120 пакетов / 2,4 секунды
декодированного PCM в каждом из пяти случаев, 10 запусков каждого этапа A/B/A2.
Пакеты заранее в RAM; сетевое аудио, demux, PDM, stage/function profiler
отсутствуют. Wi-Fi/WebUI работают, период опроса во всех сериях 15 секунд.
Task CPU включает начисленные ISR и измерительные накладные расходы;
wall также включает вытеснения. Ничего не вычитали и не отбрасывали.

| Поток, кбит/с | Контроль CPU % | bits1 CPU % | Повтор контроля CPU % | Цена на 20 мс аудио |
|---|---:|---:|---:|---:|
| mono12 | 23,092 | 23,453 | 23,073 | +72–76 мкс |
| mono24 | 54,974 | 55,283 | 54,967 | +62–63 мкс |
| stereo64 | 65,358 | 66,900 | 65,334 | +308–313 мкс |
| stereo128 | 77,381 | 79,885 | 77,373 | +501–502 мкс |
| stereo192 | 88,123 | 92,332 | 88,138 | +839–842 мкс |

На 192 кбит/с на 4,78%/4,76% больше CPU-времени относительно A/A2;
на 128 — на 3,24%/3,25%. Ни относительный, ни абсолютный критерий
не оправдывает этот кандидат. Все PCM hashes, packets/samples совпали.
30 завершённых попыток, без decoder/HTTP observation ошибок.
43 выбранных Node-регрессии прошли; host ASAN/UBSAN, mixed/PLC/reset/OOM — exact.

Максимальные отдельные wall-вызовы A/B/A2 (мкс):

| Поток | A | B | A2 |
|---|---:|---:|---:|
| mono12 | 7630 | 7427 | 9277 |
| mono24 | 15154 | 14271 | 14729 |
| stereo64 | 18943 | 18373 | 17586 |
| stereo128 | 20157 | 20868 | 19876 |
| stereo192 | 21952 | 23110 | 22514 |

Усреднённое время на 20 мс аудио не является максимальной задержкой пакета.

## Память и linked-код

App 903216→903296 B (+80); flash text 637230→637310 B; rodata 241224 B
без роста. Статические секции одинаковы: IRAM vectors/text/bss
128/22900/4044 B, DRAM data/bss 1652/18752 B. Стек задачи 5120 B,
арена 16384 B, byte-scratch 6144 B — прежние.

Физические пики word-arena/byte-scratch 15600/5488 B одинаковы;
минимальный lifetime остаток стека во всех сериях 1660 B.
Минимум sampled DRAM A/B/A2: 8352/8172/8852 B; после cleanup:
26300/26124/26292 B. Эти колебания общей кучи не доказывают утечку
или изменение постоянной памяти декодера.

| Функция | Контроль: адрес / байт | bits1: адрес / байт |
|---|---|---|
| quant_partition | 0x4024dafc / 2382 | 0x4024dafc / 2382 |
| quant_band | 0x4024e44c / 1761 | 0x4024e44c / 1793 |
| quant_all_bands | 0x402507d8 / 9416 | 0x402507f8 / 9464 |
| ec_dec_bits | 0x40298548 / 80 | 0x40298598 / 80 |

Все три быстрых пути и холодные вызовы подтверждены linked disassembly.
Третий CALL0 проверен от фактической branch target 0x40251470: линейный
objdump теряет синхронизацию на недостижимом padding по адресу 0x4025146f.
Это не повреждение инструкций. `report_tell_bits1.cjs --preflight` автоматически
проверяет эти три цели, общую correction-таблицу, hashes и совпадение RAM.

Увеличение text сдвинуло последующие функции и rodata. Отдельный вклад
исполнения макроса, положения инструкций, linker relaxation и кэша
не изолирован. Не считать регрессию доказательством именно cache misses.
Следующий независимый опыт — размещение неизменного горячего кода,
а не ещё одна комбинация нескольких алгоритмических изменений.

## Артефакты и восстановление

[Каталог эксперимента](../firmware/development/esp8266-opus-bands-tell-bits1-v1/CHANGELOG.md)
содержит binary/manifest/sdkconfig, preflight, comparison, host correctness,
43 PASS, все 30 JSON/log попыток с hashes и четыре OTA-отчёта.
`node tools/esp8266_opus_asm/report_tell_bits1.cjs` воспроизводит сводку,
проверяя параметры сборки, PCM и полноту серий.

Восстановлена обычная `esp8266-opus-live512-idle3s-20260913` через OTA,
app0 0x10000, I2S PDM GPIO3, stopped, error пустой, RSSI −62 dBm,
heap 27628 B, web-stack остаток 2324 B. Главная HTML: HTTP200 за 100 мс;
status/audio доступны. Это HTTP smoke, не полная browser QA и не проверка
20 секунд live-аудио. Production/default не менялся, UART не использовался.
