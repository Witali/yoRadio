set(YORADIO_OPUS_BACKEND "c" CACHE STRING "Opus implementation: c or diagnostic ASM backend")
option(YORADIO_OPUS_ENTROPY_IRAM_SWAP "Experiment: exchange PDM32 IRAM for three pinned ASM entropy leaves" OFF)
if(YORADIO_OPUS_ENTROPY_IRAM_SWAP AND
   (NOT YORADIO_ESP8266_DIAGNOSTIC OR NOT CONFIG_YORADIO_OGG_OPUS OR
    NOT YORADIO_OPUS_BACKEND STREQUAL "bands-tell-inline-asm" OR
    NOT YORADIO_ESP8266_PDM32_IRAM OR YORADIO_OPUS_PVQ_IRAM OR
    NOT CONFIG_YORADIO_AUDIO_OUTPUT_I2S_PDM OR NOT CONFIG_YORADIO_I2S_PDM_OVERSAMPLE_32))
  message(FATAL_ERROR "Entropy IRAM exchange requires diagnostic tell-inline ASM, PDM32 IRAM control, no PVQ placement")
endif()
option(YORADIO_OPUS_BANDS_TEXT_LITERALS "Experiment: place tell-inline bands literal pools in text" OFF)
if(YORADIO_OPUS_BANDS_TEXT_LITERALS AND
   (NOT YORADIO_ESP8266_DIAGNOSTIC OR NOT YORADIO_OPUS_BACKEND STREQUAL "bands-tell-inline-asm"))
  message(FATAL_ERROR "Bands text literals require diagnostic bands-tell-inline-asm")
endif()
set_property(CACHE YORADIO_OPUS_BACKEND PROPERTY STRINGS c gcc-asm optimized-asm hoisted-asm bands-intensity-asm bands-blocks-asm bands-combined-asm bands-pulse-lookup-asm bands-tell-inline-asm bands-fused-asm bands-tell-intensity-asm bands-update-fast-asm bands-tell-update-asm bands-tell-bits1-asm bands-layout32-asm bands-layout128-asm bands-cache-reuse-asm bands-inner4-asm bands-logp-asm bands-pvq-addx-asm bands-small-div-asm bands-small-div-tail-asm bands-small-div-inline-asm)
if(NOT YORADIO_OPUS_BACKEND MATCHES "^(c|gcc-asm|optimized-asm|hoisted-asm|bands-intensity-asm|bands-blocks-asm|bands-combined-asm|bands-pulse-lookup-asm|bands-tell-inline-asm|bands-fused-asm|bands-tell-intensity-asm|bands-update-fast-asm|bands-tell-update-asm|bands-tell-bits1-asm|bands-layout32-asm|bands-layout128-asm|bands-cache-reuse-asm|bands-inner4-asm|bands-logp-asm|bands-pvq-addx-asm|bands-small-div-asm|bands-small-div-tail-asm|bands-small-div-inline-asm)$")
  message(FATAL_ERROR "Unknown YORADIO_OPUS_BACKEND")
endif()
if(NOT CONFIG_YORADIO_OGG_OPUS AND NOT YORADIO_OPUS_BACKEND STREQUAL "c")
  message(FATAL_ERROR "Opus ASM backend requires CONFIG_YORADIO_OGG_OPUS")
endif()
if(CONFIG_YORADIO_OGG_OPUS)
  option(YORADIO_OPUS_FUNCTION_PROFILE "Diagnostic external function CPU/wall/call profiler" OFF)
  option(YORADIO_OPUS_FUNCTION_PROFILE_COARSE "Measure only root, bands, MDCT and FFT" OFF)
  if(YORADIO_OPUS_FUNCTION_PROFILE_COARSE AND NOT YORADIO_OPUS_FUNCTION_PROFILE)
    message(FATAL_ERROR "Coarse function profile requires function profiling")
  endif()
  if(YORADIO_OPUS_FUNCTION_PROFILE AND
     (NOT YORADIO_ESP8266_DIAGNOSTIC OR NOT YORADIO_ESP8266_OPUS_BENCHMARK OR
      YORADIO_ESP8266_OPUS_BENCHMARK_OUTPUT OR YORADIO_OPUS_PROFILE_STAGE OR
      NOT CONFIG_ESP8266_DEFAULT_CPU_FREQ_160))
    message(FATAL_ERROR "Function profile requires fixed160 diagnostic raw-only Opus, no stage profile")
  endif()
  # ldgen's object-symbol mappings understand foo.c.obj, but not foo.c.s.obj.
  # Replace the .s extension for generated units; instructions are unchanged.
  set(CMAKE_ASM_OUTPUT_EXTENSION_REPLACE 1)
  option(YORADIO_OPUS_PVQ_IRAM "Diagnostic placement of decode_pulses/alg_unquant in IRAM" OFF)
  set(OPUS_LDFRAGMENTS "")
  if(YORADIO_OPUS_ENTROPY_IRAM_SWAP)
    list(APPEND OPUS_LDFRAGMENTS "opus_entropy_iram.lf")
  endif()
  if(YORADIO_OPUS_PVQ_IRAM)
    if(NOT YORADIO_ESP8266_DIAGNOSTIC)
      message(FATAL_ERROR "PVQ IRAM placement requires diagnostic firmware until board qualification")
    endif()
    list(APPEND OPUS_LDFRAGMENTS "opus_pvq_iram.lf")
  endif()
  option(YORADIO_OPUS_LOW_RAM "Diagnostic SILK loan, compact autocorrelation and PLC IRAM" OFF)
  if(YORADIO_OPUS_LOW_RAM AND NOT YORADIO_ESP8266_DIAGNOSTIC)
    message(FATAL_ERROR "Low-RAM Opus requires diagnostic firmware until device qualification")
  endif()
  option(YORADIO_OPUS_PCM_LEASES "Experimental per-frame PCM ownership API" OFF)
  if(YORADIO_OPUS_PCM_LEASES AND NOT YORADIO_ESP8266_DIAGNOSTIC)
    message(FATAL_ERROR "PCM leases require diagnostic firmware until queue qualification")
  endif()
  option(YORADIO_OPUS_WORD_ASM "A/B: private Opus LX106 word accesses without volatile-C MEMW" OFF)
  option(YORADIO_OPUS_ICDF_FLASH_WORD "A/B: decode static flash ICDF tables with word loads" OFF)
  option(YORADIO_OPUS_FIR_FLASH_WORD "A/B: SILK resampler flash coefficients as word pairs" OFF)
  option(YORADIO_OPUS_CELT_DECODE_ONLY "A/B: specialize CELT bands for decoding" OFF)
  option(YORADIO_OPUS_ROTATION_LX106 "A/B: exact adjacent CELT rotation in LX106 assembly" OFF)
  option(YORADIO_OPUS_DIV_ONCE "A/B: evaluate CELT division reciprocal once" OFF)
  set(YORADIO_OPUS_PROFILE_STAGE 0 CACHE STRING "Diagnostic selected Opus stage, 0 off / 1..11")
  if(NOT YORADIO_OPUS_PROFILE_STAGE MATCHES "^([0-9]|10|11)$")
    message(FATAL_ERROR "Opus profile stage must be 0..11")
  endif()
  if(YORADIO_OPUS_PROFILE_STAGE GREATER 0 AND
     (NOT YORADIO_ESP8266_DIAGNOSTIC OR NOT YORADIO_ESP8266_OPUS_BENCHMARK OR YORADIO_ESP8266_OPUS_BENCHMARK_OUTPUT))
    message(FATAL_ERROR "Opus stage profile requires diagnostic raw-only benchmark")
  endif()
  if(YORADIO_OPUS_PROFILE_STAGE GREATER 0 AND NOT CONFIG_ESP8266_DEFAULT_CPU_FREQ_160)
    message(FATAL_ERROR "Opus stage experiment requires the matched fixed CPU160 benchmark profile")
  endif()
  file(GLOB OPUS_SOURCES upstream/src/*.c upstream/celt/*.c
       upstream/silk/*.c upstream/silk/fixed/*.c)
  if(NOT YORADIO_OPUS_BACKEND STREQUAL "c")
    include(asm/select.cmake)
  endif()
  if(YORADIO_OPUS_BANDS_TEXT_LITERALS)
    # Placement-only: pinned ASM, ABI and arithmetic are not regenerated.
    # Apply to exactly one translation unit, not all decoder literals.
    set(OPUS_BANDS_LITERAL_UNITS 0)
    foreach(src IN LISTS OPUS_SOURCES)
      if(src MATCHES "/bands-tell-inline/upstream/celt/bands[.]c[.]s$")
        set_property(SOURCE "${src}" APPEND PROPERTY COMPILE_OPTIONS "-Wa,--text-section-literals")
        math(EXPR OPUS_BANDS_LITERAL_UNITS "${OPUS_BANDS_LITERAL_UNITS}+1")
      endif()
    endforeach()
    if(NOT OPUS_BANDS_LITERAL_UNITS EQUAL 1)
      message(FATAL_ERROR "Expected exactly one pinned bands ASM unit")
    endif()
  endif()
  idf_component_register(SRCS ${OPUS_SOURCES}
      opus_memory.c opus_stage_profile.c opus_rotation_lx106.S ogg_opus_demux.c native_opus.c
      INCLUDE_DIRS . upstream/include
      PRIV_INCLUDE_DIRS upstream/celt upstream/silk upstream/silk/fixed upstream/src
      LDFRAGMENTS ${OPUS_LDFRAGMENTS})
  if(YORADIO_OPUS_FUNCTION_PROFILE)
    target_sources(${COMPONENT_LIB} PRIVATE opus_function_profile.c opus_function_wrappers.c)
    target_compile_definitions(${COMPONENT_LIB} PUBLIC YORADIO_OPUS_FUNCTION_PROFILE=1)
    set(OPUS_PROFILE_SYMBOLS yoradio_opus_decode_bounded celt_decode_with_ec quant_all_bands alg_unquant
        decode_pulses renormalise_vector clt_mdct_backward_c opus_fft_impl ec_decode
        ec_dec_update ec_dec_uint ec_dec_icdf ec_dec_bits ec_dec_bit_logp)
    if(YORADIO_OPUS_FUNCTION_PROFILE_COARSE)
      # Do not wrap frequent leaves at all: even a disabled wrapper adds cost.
      set(OPUS_PROFILE_SYMBOLS yoradio_opus_decode_bounded quant_all_bands
          clt_mdct_backward_c opus_fft_impl)
    endif()
    foreach(fn IN LISTS OPUS_PROFILE_SYMBOLS)
      target_link_libraries(${COMPONENT_LIB} INTERFACE "-Wl,--wrap=${fn}")
    endforeach()
  endif()
  target_compile_definitions(${COMPONENT_LIB} PRIVATE YORADIO_OPUS_BOUNDED=1)
  if(YORADIO_OPUS_LOW_RAM)
    # scratch-mark ABI is shared with any consumer of opus_memory.h.
    target_compile_definitions(${COMPONENT_LIB} PUBLIC YORADIO_OPUS_CELT_SILK_SCRATCH=1)
    target_compile_definitions(${COMPONENT_LIB} PRIVATE YORADIO_OPUS_AUTOCORR_COMPACT=1 YORADIO_OPUS_SILK_PLC_IRAM=1)
  endif()
  if(YORADIO_OPUS_PCM_LEASES)
    target_compile_definitions(${COMPONENT_LIB} PUBLIC YORADIO_OPUS_PCM_LEASES=1)
  endif()
  target_compile_definitions(${COMPONENT_LIB} PUBLIC YORADIO_OPUS_PROFILE_STAGE=${YORADIO_OPUS_PROFILE_STAGE})
  if(YORADIO_OPUS_WORD_ASM)
    target_compile_definitions(${COMPONENT_LIB} PRIVATE YORADIO_OPUS_WORD_ASM=1)
  endif()
  if(YORADIO_OPUS_ICDF_FLASH_WORD)
    target_compile_definitions(${COMPONENT_LIB} PRIVATE YORADIO_OPUS_ICDF_FLASH_WORD=1)
  endif()
  if(YORADIO_OPUS_FIR_FLASH_WORD)
    target_compile_definitions(${COMPONENT_LIB} PRIVATE YORADIO_OPUS_FIR_FLASH_WORD=1)
  endif()
  if(YORADIO_OPUS_CELT_DECODE_ONLY)
    target_compile_definitions(${COMPONENT_LIB} PRIVATE YORADIO_OPUS_CELT_DECODE_ONLY=1)
  endif()
  if(YORADIO_OPUS_ROTATION_LX106)
    target_compile_definitions(${COMPONENT_LIB} PRIVATE YORADIO_OPUS_ROTATION_LX106=1)
  endif()
  if(YORADIO_OPUS_DIV_ONCE)
    target_compile_definitions(${COMPONENT_LIB} PRIVATE YORADIO_OPUS_DIV_ONCE=1)
  endif()
  target_compile_options(${COMPONENT_LIB} PRIVATE -O3 -fwrapv -fstack-usage)
  # The SDK sets -mlongcalls for C/C++, but not ASM. Preserve the GCC export
  # assembler relaxation for calls from flash to ROM/IRAM and libgcc.
  if(NOT YORADIO_OPUS_BACKEND STREQUAL "c")
    target_compile_options(${COMPONENT_LIB} PRIVATE "$<$<COMPILE_LANGUAGE:ASM>:-mlongcalls>" "$<$<COMPILE_LANGUAGE:ASM>:-g0>")
  endif()
else()
  idf_component_register()
endif()
