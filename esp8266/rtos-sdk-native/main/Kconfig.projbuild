menu "yoRadio ESP8266 options"

config YORADIO_HELIX_AAC
    bool "Enable Helix AAC decoder"
    default n
    help
        Build AAC support into the ESP8266 firmware. Keep this disabled in
        the low-memory MP3 profile: the AAC decoder increases IRAM pressure
        and makes the large MP3 synthesis allocation less reliable.

config YORADIO_WEB_SEND_TIMEOUT_SECONDS
    int "Web content send timeout (seconds)"
    range 5 120
    default 20
    help
        Maximum time a static-content worker waits for one socket send.

config YORADIO_OLED
    bool "Enable SSD1306 OLED display"
    default n
    help
        Build the optional SSD1306 display task. Keep this disabled for the
        lowest-RAM WebUI-only firmware profile.

endmenu
