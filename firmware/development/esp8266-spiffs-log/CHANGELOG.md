# 2026-09-09 — локальное логирование в SPIFFS (ON)

Исходники `4005e04`. Обычный native radio-профиль: CPU160, QIO40,
MP3 Helix SSO + AAC, вход 4096 байт, I2S PDM32 GPIO3, DMA 2×512 слов,
короткая HTTP-пауза, уровень логирования ERROR.

Добавлено `YORADIO_ESP8266_SPIFFS_LOG=ON`: SDK ESP_LOGx дублируется в
512-байтную очередь, затем app task сохраняет до 256 байт в секунду в
два ротируемых файла по 8 КиБ. UART сохранён. HTTP-выдачи журнала нет;
историю можно извлечь позже из SPIFFS через USB/копию flash.
Защита от заполнения раздела, рекурсивных ошибок, циклических повторов
и дальнейших открытий после ошибки SPIFFS close.

763328 байт, +1504 к OFF. Статическая граница DRAM увеличилась на 552 байта;
IRAM не изменилась. Хеши — в manifest.json. 250 host-тестов passed.
Первоначально проверена сборкой и host-тестами.

## Последующая установка на плату — 2026-09-09

По запросу пользователя установлена через OTA: слот 0x110000 → 0x10000,
HTTP 200/OK, запуск нового слота подтверждён. NVS/SPIFFS не перезаписывались
образом; штатная запись логов в SPIFFS включена. HTTP-доступа к логам нет.
Журнал сохраняет ERROR, без отдельного MemoryProfile/verbose.

WebUI: одна тестовая вкладка, 511 станций; загрузка 388 мс, 10 команд
громкости с ACK 23–52 мс, громкость восстановлена в 128.
Оставлена запущенной «Европа Плюс», MP3 128 кбит/с, I2S PDM32 GPIO3.
За 22,38 с PCM и DMA продвигались, но появились 1022 underrun и PCM/time
94,36%: continuity FAIL. Это не доказательство чистого непрерывного звука
или влияния именно логгера. Свободная heap в выборке 7528–9464 байта,
исторический минимум 5588. Начальный RSSI −78 dBm, во время аудиозамера
от −78 до −65 dBm. Пользователь планирует сравнение ближе к роутеру.
Содержимое файлов журнала не извлекалось; это будет отдельное чтение по USB.
