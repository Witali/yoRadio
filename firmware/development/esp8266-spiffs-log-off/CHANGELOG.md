# 2026-09-09 — контрольный профиль SPIFFS logging OFF

Исходники `4005e04`. Те же CPU160/QIO40, MP3 Helix SSO + AAC, вход 4096 байт,
I2S PDM32 GPIO3 и короткая HTTP-пауза, но журналирование SPIFFS выключено.
Это значение по умолчанию. Код, очередь и операции записи журнала исключены;
имеющиеся на flash файлы логов при этом не удаляются.

761824 байта; хеши — в manifest.json. 250 host-тестов passed.
На плату не прошивалась: это контрольная сборка для сравнения ON/OFF.
